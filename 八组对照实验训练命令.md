# 八组对照实验训练命令

本文档用于记录当前三分类实验的 8 组对照设置，以及在服务器上运行的训练命令。

> 说明：你口头说的 `tft` 在当前代码参数中对应 `fft`；`time_tft` 对应 `time_fft`。

## 1. 八组实验总表

| 组别 | 实验目的 | input-domain | conv-type | fft-global | 模型形式 | 输出目录 |
| --- | --- | --- | --- | --- | --- | --- |
| A1 | time baseline | `time` | `standard` | `none` | 时域单分支 | `local_artifacts/outputs/activity_loso_time` |
| A2 | fft baseline | `fft` | `standard` | `none` | FFT 单分支 | `local_artifacts/outputs/activity_loso_fft` |
| A3 | time+fft baseline | `time_fft` | `standard` | `none` | 时域 + FFT 双分支 | `local_artifacts/outputs/activity_loso_time_fft` |
| B1 | time DWConv | `time` | `dwconv` | `none` | 时域单分支 + DWConv | `local_artifacts/outputs/activity_loso_time_dwconv` |
| B2 | fft DWConv | `fft` | `dwconv` | `none` | FFT 单分支 + DWConv | `local_artifacts/outputs/activity_loso_fft_dwconv` |
| B3 | time+fft DWConv | `time_fft` | `dwconv` | `none` | 双分支 + DWConv | `local_artifacts/outputs/activity_loso_time_fft_dwconv` |
| C1 | fft DWConv + MLP | `fft` | `dwconv` | `mlp` | FFT 单分支 + DWConv + FFT MLP | `local_artifacts/outputs/activity_loso_fft_dwconv_mlp` |
| C2 | time+fft DWConv + MLP | `time_fft` | `dwconv` | `mlp` | 双分支 + DWConv + FFT 分支 MLP | `local_artifacts/outputs/activity_loso_time_fft_dwconv_mlp` |

## 2. 服务器训练命令

先进入项目目录：

```bash
cd /public/home/gd_110413/SWT/work/eeg
```

---

### A1. time 单分支 baseline

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time \
  --conv-type standard \
  --fft-global none \
  --output-base local_artifacts/outputs/activity_loso_time
```

### A2. fft 单分支 baseline

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain fft \
  --conv-type standard \
  --fft-global none \
  --output-base local_artifacts/outputs/activity_loso_fft
```

### A3. time+fft 双分支 baseline

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --output-base local_artifacts/outputs/activity_loso_time_fft
```

### A3-depth. time+fft TransformerEncoder Block 数量消融

`--depth` 控制每个 `TransformerEncoder` 的 Block 数量；双分支模型中时域、FFT 两个编码器都会使用该值。以下命令固定其他 A3 参数，测试 1、2、3、4、6、8 个 Block，并为每个深度写入独立目录，避免覆盖已有结果：

```bash
for depth in 1 2 3 4 6 8; do
  ./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
    --device cuda:0 \
    --epochs 200 \
    --batch-size 72 \
    --class-weights 3,3,1 \
    --input-domain time_fft \
    --conv-type standard \
    --fft-global none \
    --depth "${depth}" \
    --output-base "local_artifacts/outputs/activity_loso_time_fft_depth_${depth}" \
    --skip-existing
done
```

---

### B1. time 单分支 DWConv

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time \
  --conv-type dwconv \
  --fft-global none \
  --output-base local_artifacts/outputs/activity_loso_time_dwconv
```

### B2. fft 单分支 DWConv

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain fft \
  --conv-type dwconv \
  --fft-global none \
  --output-base local_artifacts/outputs/activity_loso_fft_dwconv
```

### B3. time+fft 双分支 DWConv

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type dwconv \
  --fft-global none \
  --output-base local_artifacts/outputs/activity_loso_time_fft_dwconv
```

---

### C1. fft 单分支 DWConv + FFT MLP

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain fft \
  --conv-type dwconv \
  --fft-global mlp \
  --output-base local_artifacts/outputs/activity_loso_fft_dwconv_mlp
```

### C2. time+fft 双分支 DWConv + FFT MLP

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type dwconv \
  --fft-global mlp \
  --output-base local_artifacts/outputs/activity_loso_time_fft_dwconv_mlp
```

## 3. 建议运行顺序

如果算力充足，直接按 A1 → A2 → A3 → B1 → B2 → B3 → C1 → C2 顺序跑。

如果想先看主结论，优先跑：

```text
A1 time standard
A2 fft standard
A3 time_fft standard
B3 time_fft dwconv
C2 time_fft dwconv mlp
```

## 4. 输出结果位置

每组实验会在对应输出目录下，按窗口/步长生成子目录，例如：

```text
local_artifacts/outputs/activity_loso_time_fft_dwconv_mlp/window_15_stride_3/
```

每个窗口目录里重点查看：

```text
summary.json
summary.csv
experiment_manifest.json
experiment_manifest.md
```

每个 fold 目录里重点查看：

```text
fold_subject_<id>/metrics.json
fold_subject_<id>/best_model.pt
fold_subject_<id>/epoch_history.json
```


## 5. A3-QKV 改进实验：只跑 `window_15_stride_3`

> 注意：下面命令每一行的 `\` 必须是该行最后一个字符，后面不能有空格。

如果当前目录是项目根目录：

```bash
cd /public/home/gd_110413/SWT/work/eeg

./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --input-qkv channel \
  --input-qkv-dim 64 \
  --input-qkv-heads 4 \
  --input-qkv-dropout 0.1 \
  --input-qkv-res-scale 0.1 \
  --epochs 200 \
  --batch-size 72 \
  --device cuda:0 \
  --output-dir local_artifacts/outputs/activity_loso_A3_qkv/window_15_stride_3
```

如果当前目录已经是 `EEG-Conformer`：

```bash
cd /public/home/gd_110413/SWT/work/eeg/EEG-Conformer

../.conda-envs/eegconformer310/bin/python train_activity_loso_batch.py \
  --dataset-root ../local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --input-qkv channel \
  --input-qkv-dim 64 \
  --input-qkv-heads 4 \
  --input-qkv-dropout 0.1 \
  --input-qkv-res-scale 0.1 \
  --epochs 200 \
  --batch-size 72 \
  --device cuda:0 \
  --output-dir ../local_artifacts/outputs/activity_loso_A3_qkv/window_15_stride_3
```