# 三分类训练命令笔记

当前不需要再手动改 `train_activity_loso.py` 里的 loss。类别权重已经支持命令行参数：

```bash
--class-weights 3,3,1
```

当前输入形式主要有三种：

```bash
--input-domain time       # 时域单分支
--input-domain fft        # FFT 频域单分支
--input-domain time_fft   # 时域 + FFT 双分支
```

双分支含义：

```text
时域 EEG 分支 + FFT 频域分支 -> 特征拼接 -> 三分类
```

当前卷积形式有两种：

```bash
--conv-type standard   # 默认，旧实验使用的普通 Conv2d
--conv-type dwconv     # 新增，空间卷积使用 depthwise conv
```

FFT 输入还可以额外加一个轻量全局 MLP：

```bash
--fft-global none      # 默认，不加
--fft-global mlp       # 只作用于 fft / time_fft 里的 FFT 输入，前后 shape 不变
```

输入进入原 EEG-Conformer 前，还可以加轻量 QKV residual：

```bash
--input-qkv none       # 默认，不加，旧流程不变
--input-qkv channel    # 把 EEG 通道当 token 做 QKV attention，再 residual 加回输入
--input-qkv time       # 把时间/频率片段当 token 做 QKV attention，再 residual 加回输入
```

TransformerEncoder 内部还可以启用跨 Block 累计 Query 注意力：

```bash
--cumulative-query-attention  # 默认关闭；启用后第 i 层使用 (Q1+...+Qi)Ki^T
```

如果要做 DWConv 对照实验，重点看下面第 4 节；如果要做 DWConv + FFT MLP，对照看第 4.1 节；如果要做 A3-QKV channel/time 改进实验，看第 4.2 节；如果要做 Transformer 6 个 Block 累计 Q 实验，看第 4.3 节。

---

## 1. Mac 本地推荐命令：双分支 time_fft

```bash
cd /Users/swt/Desktop/eeg
./.conda-envs/eeg-mac-test-train/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device mps \
  --epochs 200 \
  --batch-size 16 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --output-base local_artifacts/outputs/activity_loso_time_fft
```

如果 Mac 显存/内存不够，把 batch size 降低：

```bash
--batch-size 8
```

---

## 2. 服务器推荐命令：双分支 time_fft

```bash
cd /public/home/gd_110413/SWT/work/eeg
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --output-base local_artifacts/outputs/activity_loso_time_fft
```

如果 CUDA 显存不够，优先改：

```bash
--batch-size 32
```

或者：

```bash
--batch-size 16
```

---

### 2.1 `window_15_stride_3`：A3 time_fft TransformerEncoder Block 数量消融（1～10）

此实验固定使用 `八组对照_全部窗口步长_训练结果对比.md` 总表第 2 名的配置：**A3 time+fft 双分支 standard**、`window_15_stride_3`、200 epochs、batch size 72、class weights `3,3,1`。不再遍历全部窗口/步长。

`--depth` 控制每个 `TransformerEncoder` 的 Block 数。`time_fft` 是双分支模型，因此 time 分支和 FFT 分支都会各自使用相同的 Block 数。以下命令依次训练 1～10 个 Block；每个深度使用独立输出目录，避免结果互相覆盖。`--skip-existing` 只负责跳过完整 fold；需要从未完成 epoch 连同 Adam optimizer 状态继续时，还要启用 `--resume`。

每次启动训练时将 PyTorch/NumPy 相关 CPU 计算线程限制为 `8`，避免默认线程池创建过多线程、与 GPU 数据供给和调度相互竞争。该限制只对当前 Python 训练进程生效；如果同时在两张 GPU 上各跑一个任务，建议两个命令都改为 `6`。

```bash
cd /public/home/gd_110413/SWT/work/eeg

for depth in $(seq 18 20); do
  OMP_NUM_THREADS=8 MKL_NUM_THREADS=8 OPENBLAS_NUM_THREADS=8 NUMEXPR_NUM_THREADS=8 \
  ./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
    --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
    --device cuda:1 \
    --epochs 200 \
    --batch-size 72 \
    --class-weights 3,3,1 \
    --input-domain time_fft \
    --conv-type standard \
    --fft-global none \
    --depth "${depth}" \
    --output-dir "local_artifacts/outputs/activity_loso_time_fft_depth_${depth}/window_15_stride_3" \
    --skip-existing
done
```

种子版
```bash
seed=44 
for depth in $(seq 10 12); do
  OMP_NUM_THREADS=8 MKL_NUM_THREADS=8 OPENBLAS_NUM_THREADS=8 NUMEXPR_NUM_THREADS=8 \
  ./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
    --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
    --device cuda:1 \
    --epochs 200 \
    --batch-size 72 \
    --class-weights 3,3,1 \
    --input-domain time_fft \
    --conv-type standard \
    --fft-global none \
    --depth "${depth}" \
    --seed "${seed}" \
    --output-dir "local_artifacts/outputs/activity_loso_time_fft_depth_${depth}/window_15_stride_3_seed_${seed}" \
    --skip-existing
done
```



输出目录：

```text
local_artifacts/outputs/activity_loso_time_fft_depth_1/window_15_stride_3
...
local_artifacts/outputs/activity_loso_time_fft_depth_10/window_15_stride_3
```

---

## 3. 普通卷积 baseline 对照实验命令

不写 `--conv-type` 时默认就是 `standard`。这里显式写出来，方便和 DWConv 实验区分。

### 只用时域 time，单分支 standard

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time \
  --conv-type standard \
  --output-base local_artifacts/outputs/activity_loso_time
```

### 只用 FFT 频域，单分支 standard

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain fft \
  --conv-type standard \
  --output-base local_artifacts/outputs/activity_loso_fft
```

### 时域 + FFT 双分支，standard

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --output-base local_artifacts/outputs/activity_loso_time_fft
```

---

## 4. DWConv 对照实验命令

这三条是新增 DWConv 实验，输出目录和旧的 `activity_loso_time_fft` 明确区分。

### 只用时域 time，单分支 DWConv

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time \
  --conv-type dwconv \
  --output-base local_artifacts/outputs/activity_loso_time_dwconv
```

### 只用 FFT 频域，单分支 DWConv

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain fft \
  --conv-type dwconv \
  --output-base local_artifacts/outputs/activity_loso_fft_dwconv
```

### 时域 + FFT 双分支，DWConv

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type dwconv \
  --output-base local_artifacts/outputs/activity_loso_time_fft_dwconv
```

说明：对 `train_activity_loso_generated_batch.py`，如果使用 `--conv-type dwconv` 且不手动写 `--output-base`，脚本也会自动使用上面这种 `activity_loso_<domain>_dwconv` 目录；但为了命令清楚，笔记里仍然显式写出。

### 4.1 DWConv + FFT 全局 MLP 实验命令

这个实验只对 FFT 输入加 MLP，所以主要跑 `fft` 和 `time_fft` 两组；`time` 没有 FFT 输入，不适用 `--fft-global mlp`。

#### 只用 FFT 频域，单分支 DWConv + MLP

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain fft \
  --conv-type dwconv \
  --fft-global mlp \
  --output-base local_artifacts/outputs/activity_loso_fft_dwconv_mlp
```

#### 时域 + FFT 双分支，DWConv + MLP

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type dwconv \
  --fft-global mlp \
  --output-base local_artifacts/outputs/activity_loso_time_fft_dwconv_mlp
```

### 4.2 QKV residual 改进实验命令

这个实验是在输入进入原 EEG-Conformer 之前，先加一层轻量 QKV attention，然后用跳跃连接加回原输入：

```text
x -> QKV attention -> gamma * qkv_out
x + gamma * qkv_out -> 原 EEG-Conformer
```

默认 `gamma=0.1`，对应参数 `--input-qkv-res-scale 0.1`。它是可学习参数，可以改初始值。

#### 新增推荐：A3-QKV-time，time+fft 双分支 standard

这是针对当前 A3：`time_fft + standard + fft_global none` 的新版 QKV。它把时间/频率片段当 token，而不是把 21 个电极当 token。

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --input-qkv time \
  --input-qkv-dim 64 \
  --input-qkv-heads 4 \
  --input-qkv-dropout 0.1 \
  --input-qkv-res-scale 0.1 \
  --output-base local_artifacts/outputs/activity_loso_time_fft_qkv_time
```

注意：`time_fft` 双分支里，time 分支和 FFT 分支各自做 QKV；两个 QKV 模块不共享参数，也不是跨 time/FFT 的 cross-attention。

#### 对照保留：A3-QKV-channel

旧版 QKV 把 21 个电极当 token，可作为对照：

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --input-qkv channel \
  --input-qkv-dim 64 \
  --input-qkv-heads 4 \
  --input-qkv-dropout 0.1 \
  --input-qkv-res-scale 0.1 \
  --output-base local_artifacts/outputs/activity_loso_time_fft_qkv_channel
```

#### 只跑 `window_15_stride_3`：A3-QKV-time

如果只想先验证 `window_15_stride_3`，可以直接用单数据集 batch 脚本：

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --input-qkv time \
  --input-qkv-dim 64 \
  --input-qkv-heads 4 \
  --input-qkv-dropout 0.1 \
  --input-qkv-res-scale 0.1 \
  --output-dir local_artifacts/outputs/activity_loso_A3_qkv_time/window_15_stride_3
```

---

### 4.3 TransformerEncoder 跨 Block 累计 Q 实验

这个实验不改 PatchEmbedding、K、V、FeedForward 和分类头，只修改 6 个 Transformer Block 的 attention Energy：

```text
普通模式：
Block1: Q1 K1^T
Block2: Q2 K2^T
Block3: Q3 K3^T
...
Block6: Q6 K6^T

启用 --cumulative-query-attention：
Block1: Q1 K1^T
Block2: (Q1 + Q2) K2^T
Block3: (Q1 + Q2 + Q3) K3^T
...
Block6: (Q1 + Q2 + Q3 + Q4 + Q5 + Q6) K6^T
```

每个 Block 仍然使用当前层自己的 `Ki` 和 `Vi`。累计 Q 只在同一次 forward 的 6 个 Block 之间传递；每个新 batch 都会重新清空。`time_fft` 模式下，time 分支和 FFT 分支分别累计，两个分支不会共享 Q。

#### 先跑单数据集 smoke test

建议先在 `window_15_stride_3` 上跑 1 个 epoch，确认显存、日志和产物正常：

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --device cuda:0 \
  --epochs 1 \
  --batch-size 16 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --input-qkv none \
  --cumulative-query-attention \
  --output-dir local_artifacts/outputs/smoke_time_fft_cumulative_q/window_15_stride_3
```

#### 正式跑全部窗口/步长

为了只比较累计 Q 的影响，第一组建议使用 `time_fft + standard + fft_global none + input_qkv none`，其他配置和 baseline 保持一致：

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --input-qkv none \
  --cumulative-query-attention \
  --output-base local_artifacts/outputs/activity_loso_time_fft_cumulative_q
```

对应的严格 baseline 命令只需要去掉：

```bash
--cumulative-query-attention
```

并把输出目录换回独立的 baseline 目录，不能让两组实验共用同一个 `output-base`。

---

## 5. 参数说明：train_activity_loso_generated_batch.py

这个脚本用于按 `window_stride_configs.json` 批量跑多套窗口配置，并对每套数据集跑完整 LOSO。

### `--input-domain`

控制模型输入形式。

| 参数值 | 含义 | 说明 |
| --- | --- | --- |
| `time` | 只用时域 EEG | 原始 EEG 窗口输入，单分支模型。 |
| `fft` | 只用频域 | 对时间轴做 rFFT，取 `log(1 + power)`，单分支模型。 |
| `time_fft` | 时域 + FFT 双分支 | 推荐主实验；两个分支独立提特征后拼接分类。 |

注意：`time_fft` 的 FFT 分支要求 FFT 后长度能通过 EEG-Conformer stem。当前 `window_3_stride_3` 可以跑；太短窗口，比如 1 秒，可能因为 FFT 后长度不足而报错。

---

### `--conv-type`

控制 PatchEmbedding 里空间卷积的形式。

| 参数值 | 含义 | 说明 |
| --- | --- | --- |
| `standard` | 普通 Conv2d | 默认值；和旧实验结构一致。 |
| `dwconv` | Depthwise spatial conv | 新增对照；空间卷积使用 `groups=40`，参数更少。 |

当前 DWConv 只改空间卷积这一层：

```python
nn.Conv2d(40, 40, (n_channels, 1), groups=40)
```

时域卷积和 `1x1 projection` 仍然是普通卷积。

建议：

```bash
# 跑旧 baseline
--conv-type standard

# 跑 DWConv 对照
--conv-type dwconv
```

`metrics.json`、`best_model.pt`、`epoch_history.json` 和 `experiment_manifest.json` 都会记录 `conv_type`。

---

### `--fft-global`

控制 FFT 输入进入 EEG-Conformer 之前是否先做一次轻量全局操作。

| 参数值 | 含义 | 说明 |
| --- | --- | --- |
| `none` | 不加 | 默认值；和旧实验一致。 |
| `mlp` | 频率维 MLP | 输入输出尺寸不变，只作用于 FFT 输入。 |

作用范围：

| input-domain | `--fft-global mlp` 是否适用 |
| --- | --- |
| `time` | 不适用，没有 FFT 输入；脚本会报错提醒。 |
| `fft` | 适用，整个输入先过 MLP。 |
| `time_fft` | 适用，只给 FFT 分支加 MLP，time 分支不变。 |

形状保持不变：

```text
(B, 1, C, F) -> FFTGlobalMLP -> (B, 1, C, F)
```

推荐只和 DWConv 组合做对照：

```bash
--input-domain fft --conv-type dwconv --fft-global mlp
--input-domain time_fft --conv-type dwconv --fft-global mlp
```

`metrics.json`、`best_model.pt`、`epoch_history.json` 和 `experiment_manifest.json` 都会记录 `fft_global`。

---

### `--input-qkv`

控制输入进入原 EEG-Conformer 前是否加 QKV residual。

| 参数值 | 含义 | 说明 |
| --- | --- | --- |
| `none` | 不加 | 默认值；旧实验完全不变。 |
| `channel` | 通道级 QKV residual | 把 EEG 通道 `C` 当 token，对通道之间做 attention，输出再加回输入。 |
| `time` | 时间片级 QKV residual | 把时间/频率轴切成短片段当 token，对时间片之间做 attention；例如 `1920 -> 60 x 32`，FFT `961 -> 31 x 31`。 |

`channel` 结构：

```text
输入 x: (B, 1, C, L)
  -> squeeze 成 (B, C, L)
  -> LayerNorm(L)
  -> Linear(L -> input_qkv_dim)
  -> MultiHeadAttention(Q,K,V)
  -> Linear(input_qkv_dim -> L)
  -> Dropout
  -> x + gamma * qkv_out
  -> 原 EEG-Conformer
```

`time` 结构：

```text
输入 x: (B, 1, C, L)
  -> 沿 L 切成 n_tokens 个片段，每个 token 特征为 C * token_len
  -> LayerNorm(C * token_len)
  -> Linear(C * token_len -> input_qkv_dim)
  -> MultiHeadAttention(Q,K,V)，注意力矩阵为 n_tokens x n_tokens
  -> Linear(input_qkv_dim -> C * token_len)
  -> 还原到 (B, 1, C, L)
  -> x + gamma * qkv_out
  -> 原 EEG-Conformer
```

`window_15_stride_3` 时域分支的典型 shape：

```text
(B, 1, 21, 1920)
→ (B, 60, 672)       # 60 个 token，每个 token = 21 * 32
→ (B, 60, 64)
→ attention: 60 x 60
→ (B, 60, 672)
→ (B, 1, 21, 1920)
```

对应 FFT 分支当前 rFFT 长度通常是 `961 = 31 x 31`：

```text
(B, 1, 21, 961)
→ (B, 31, 651)       # 31 个 token，每个 token = 21 * 31
→ (B, 31, 64)
→ attention: 31 x 31
→ (B, 31, 651)
→ (B, 1, 21, 961)
```

如果某个 FFT 长度是 `963`，QKV 模块会内部 pad 到 `31 x 32 = 992`，attention 仍是 `31 x 31`，最后再裁剪回原长度。

相关参数：

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| `--input-qkv-dim` | `64` | QKV attention 内部 embedding 宽度。 |
| `--input-qkv-heads` | `4` | attention head 数；要求 `input_qkv_dim` 能被它整除。 |
| `--input-qkv-dropout` | `0.1` | QKV 模块里的 dropout。 |
| `--input-qkv-res-scale` | `0.1` | 跳跃连接初始缩放系数 `gamma`；训练中会继续学习，可手动改初始值。 |

作用范围：

| input-domain | `--input-qkv channel/time` 作用位置 |
| --- | --- |
| `time` | time 输入进单分支前。 |
| `fft` | FFT 输入进单分支前。 |
| `time_fft` | time 分支和 FFT 分支各自进网络前；两个 QKV 模块不共享参数。 |

推荐先跑新版 time-token QKV：

```bash
--input-domain time_fft \
--conv-type standard \
--fft-global none \
--input-qkv time \
--input-qkv-dim 64 \
--input-qkv-heads 4 \
--input-qkv-dropout 0.1 \
--input-qkv-res-scale 0.1
```

`metrics.json`、`best_model.pt`、`epoch_history.json` 和 `experiment_manifest.json` 都会记录 QKV 相关参数；`--input-qkv time` 还会记录 time/FFT token 数和 token 长度。

---

### `--cumulative-query-attention`

这是一个布尔开关，默认关闭，不需要给它填写 `true/false`：

```bash
# 默认普通注意力，不写该参数

# 启用跨 Block 累计 Q
--cumulative-query-attention
```

作用位置是 PatchEmbedding 后面的 6 层 `TransformerEncoder`，不是前面的 `--input-qkv channel/time` 模块。这两个功能相互独立，也可以组合，但建议先单独测试累计 Q，避免一次改变多个变量。

默认配置 `emb_size=40`、`num_heads=5` 时，每层 Q 的形状是：

```text
(B, 5, n_tokens, 8)
```

例如 `window_15_stride_3` 的时域输入通常产生 122 个 patch token：

```text
Q_sum:    (B, 5, 122, 8)
K:        (B, 5, 122, 8)
Energy:   (B, 5, 122, 122)
```

启用后参数量和 attention 矩阵形状不变，但深层累计 Q 的数值幅度可能更大，softmax 可能更加尖锐，因此需要重点观察：

- train loss 是否正常下降；
- 是否出现 NaN/Inf；
- test accuracy、macro-F1 和各类别 recall；
- 与相同 seed、相同数据、相同超参数 baseline 的差异。

训练产物会记录：

```json
"cumulative_query_attention": true
```

记录位置包括 `metrics.json`、`best_model.pt`、最终模型 `train_config.json` 和 `experiment_manifest.json`。预测和 API 加载模型时会恢复这个配置；旧模型没有该字段时按 `false` 处理。

`--skip-existing` 会检查这个字段：已有普通 attention 结果不会被当作累计 Q 实验的已完成结果。没有手动指定 `--output-base` 时，生成数据集批量脚本会自动使用带 `cumulative_q` 的目录后缀；正式实验仍建议明确指定独立输出目录。

---

### `--epochs`

训练轮数。

| 推荐值 | 用途 |
| --- | --- |
| `1` | smoke test，只验证代码能跑通。 |
| `50` | 快速观察趋势。 |
| `100` | 中等实验。 |
| `200` | 当前推荐正式实验。 |

例如：

```bash
--epochs 200
```

---

### `--device`

控制训练设备。

| 参数值 | 使用场景 |
| --- | --- |
| `cpu` | 最稳，但很慢，适合 smoke test。 |
| `mps` | Apple Silicon Mac 使用。 |
| `cuda:0` | 服务器第一张 NVIDIA GPU。 |
| `cuda:1` | 服务器第二张 NVIDIA GPU。 |

例如：

```bash
--device mps
--device cuda:0
```

---

### `--batch-size`

每个 batch 的窗口数量。

| 推荐值 | 使用场景 |
| --- | --- |
| `8` | Mac MPS 或显存紧张。 |
| `16` | Mac MPS 常用。 |
| `32` | CUDA 显存一般时。 |
| `72` | 代码默认值，CUDA 显存够时可用。 |

如果报显存/内存不足，优先降低这个参数。

---

### `--class-weights`

类别权重，用于处理类别不平衡。

当前数据里活动 3 样本明显更多，所以推荐：

```bash
--class-weights 3,3,1
```

含义：

```text
活动 1 权重 3
活动 2 权重 3
活动 3 权重 1
```

可选写法：

| 参数 | 含义 |
| --- | --- |
| 不写 | 不使用类别权重。 |
| `--class-weights 1,1,1` | 等价于基本不加权。 |
| `--class-weights 3,3,1` | 当前推荐，增强活动 1/2。 |
| `--class-weights 4,4,1` | 更强地增强活动 1/2，可能提高召回，也可能降低整体准确率。 |

---

### `--output-base`

控制训练结果保存在哪里。

推荐不同实验放不同目录，避免混在一起。

普通卷积 baseline 目录：

```bash
--output-base local_artifacts/outputs/activity_loso_time
--output-base local_artifacts/outputs/activity_loso_fft
--output-base local_artifacts/outputs/activity_loso_time_fft
```

DWConv 对照目录：

```bash
--output-base local_artifacts/outputs/activity_loso_time_dwconv
--output-base local_artifacts/outputs/activity_loso_fft_dwconv
--output-base local_artifacts/outputs/activity_loso_time_fft_dwconv
```

DWConv + FFT MLP 对照目录：

```bash
--output-base local_artifacts/outputs/activity_loso_fft_dwconv_mlp
--output-base local_artifacts/outputs/activity_loso_time_fft_dwconv_mlp
```

QKV residual 改进目录：

```bash
--output-base local_artifacts/outputs/activity_loso_time_fft_qkv_channel
--output-base local_artifacts/outputs/activity_loso_time_fft_qkv_time
```

如果 `train_activity_loso_generated_batch.py` 使用 `--conv-type dwconv`、`--fft-global mlp` 或 `--input-qkv channel/time`，且没有手动写 `--output-base`，脚本会自动把默认输出目录改成：

```text
activity_loso_time_dwconv
activity_loso_fft_dwconv
activity_loso_time_fft_dwconv
activity_loso_fft_dwconv_mlp
activity_loso_time_fft_dwconv_mlp
activity_loso_time_fft_qkv_channel
activity_loso_time_fft_qkv_time
```

如果改了重要参数，比如：

```text
input-domain
conv-type
fft-global
input-qkv
input-qkv-dim / heads / dropout / res-scale
class-weights
epochs
window/stride
```

建议换一个新的 `output-base`，否则可能因为已有结果被跳过，或者新旧结果混在一起。

---

### `--lr`

学习率，默认：

```bash
--lr 0.0002
```

一般先不动。可尝试：

| 参数 | 说明 |
| --- | --- |
| `--lr 0.0002` | 默认推荐。 |
| `--lr 0.0001` | 更保守，训练更慢但可能更稳。 |
| `--lr 0.0005` | 更激进，可能更快，也可能不稳定。 |

---

### `--config`

窗口/步长配置文件，默认：

```bash
--config eeg-data-processing/data_to_list/window_stride_configs.json
```

这个文件决定要跑哪些数据集，例如：

```json
[
  {"window_seconds": 3.0, "stride_seconds": 3.0}
]
```

注意：训练脚本只读取已经生成好的数据集。如果改了 config，需要先生成对应数据集。

---

### `--dataset-base`

全局数据集根目录，默认：

```bash
--dataset-base local_artifacts/data_to_list/global_activity_dataset
```

脚本会在里面找：

```text
window_3_stride_3/
window_5_stride_5/
...
```

一般不用改，除非服务器数据放在其他位置。

---

### `--skip-existing` / `--no-skip-existing`

默认是：

```bash
--skip-existing
```

也就是已有完整结果会跳过。现在跳过检查会同时看：

```text
input_domain
conv_type
fft_global
input_qkv
input_qkv_dim
input_qkv_heads
input_qkv_dropout
input_qkv_res_scale
```

所以同一个目录里，`standard`、`dwconv`、`dwconv+mlp`、`qkv_channel`、`qkv_time` 不会被误认为是同一个实验；但仍然建议分目录保存。

如果想强制重跑：

```bash
--no-skip-existing
```

但更推荐换新的 `--output-base`，避免覆盖或混淆旧结果。

### `--resume`

```bash
--resume
```

启用后，每个 epoch 会原子更新 `fold_subject_<id>/last_checkpoint.pt`，保存模型、Adam optimizer、最佳指标、epoch history 和随机数状态。重新执行相同命令时可从下一个 epoch 继续；批量 LOSO 建议与 `--skip-existing` 同时使用。fold 完成并生成 `metrics.json` 后会自动删除恢复 checkpoint。

旧版 `best_model.pt` 没有 optimizer 状态，不能用于真正的 optimizer resume；如果目录中没有新版 `last_checkpoint.pt`，该 fold 会从 epoch 1 重跑。

---

### `--seed`

随机种子，默认：

```bash
--seed 42
```

用于控制随机初始化和训练可复现性。一般不动；如果想做多随机种子实验，可以尝试：

```bash
--seed 1
--seed 2
--seed 3
```

并配不同的 `output-base`。

---

## 6. 常用组合

### 快速 smoke test

```bash
./.conda-envs/eeg-mac-test-train/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cpu \
  --epochs 1 \
  --batch-size 4 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --output-base local_artifacts/outputs/smoke_time_fft
```

### Mac 正式双分支

```bash
./.conda-envs/eeg-mac-test-train/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device mps \
  --epochs 200 \
  --batch-size 16 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --output-base local_artifacts/outputs/activity_loso_time_fft
```

### 服务器正式双分支 baseline

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --output-base local_artifacts/outputs/activity_loso_time_fft
```

### 服务器正式双分支 DWConv

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type dwconv \
  --output-base local_artifacts/outputs/activity_loso_time_fft_dwconv
```

### 服务器正式双分支 DWConv + FFT MLP

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type dwconv \
  --fft-global mlp \
  --output-base local_artifacts/outputs/activity_loso_time_fft_dwconv_mlp
```

### 服务器 A3-QKV-time：只跑 `window_15_stride_3`

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --input-qkv time \
  --input-qkv-dim 64 \
  --input-qkv-heads 4 \
  --input-qkv-dropout 0.1 \
  --input-qkv-res-scale 0.1 \
  --output-dir local_artifacts/outputs/activity_loso_A3_qkv_time/window_15_stride_3
```

### 服务器 A3-QKV-time：跑全部窗口步长

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --input-qkv time \
  --input-qkv-dim 64 \
  --input-qkv-heads 4 \
  --input-qkv-dropout 0.1 \
  --input-qkv-res-scale 0.1 \
  --output-base local_artifacts/outputs/activity_loso_time_fft_qkv_time
```

### 服务器累计 Q：只跑 `window_15_stride_3`

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --input-qkv none \
  --cumulative-query-attention \
  --output-dir local_artifacts/outputs/activity_loso_time_fft_cumulative_q/window_15_stride_3
```

### 服务器累计 Q：跑全部窗口步长

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --input-qkv none \
  --cumulative-query-attention \
  --output-base local_artifacts/outputs/activity_loso_time_fft_cumulative_q
```

### 服务器多深度 Transformer 可学习加权：只跑 `window_15_stride_3`

Time 和 FFT 两侧分别并行使用 depth=11、10、8 的 TransformerEncoder，并在各自进入 Flatten 前使用独立的 Softmax 可学习权重加权求和。默认 seed=42，其余参数与 A3 time+fft 双分支 standard 保持一致。

```bash
OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12 \
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --device cuda:1 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --transformer-branches 3 \
  --transformer-depths 11 10 8 \
  --seed 43 \
  --output-dir local_artifacts/outputs/activity_loso_time_fft_multidepth_11_10_8_seed43/window_15_stride_3 \
  --skip-existing
```

---

### 服务器多深度 Transformer 分支 Loss 可学习融合：只跑 `window_15_stride_3`

该模式将相同深度的 time/FFT 特征配对，分别经过独立分类头得到三个 CrossEntropyLoss，使用可学习 Softmax 权重合并。`--branch-loss-aux-weight 0.2` 为每个分支保留基础监督，降低单一分支权重垄断的风险。

```bash
OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12 \
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --transformer-branches 3 \
  --transformer-depths 11 10 8 \
  --transformer-branch-fusion loss_softmax \
  --branch-loss-aux-weight 0.2 \
  --seed 42 \
  --output-dir local_artifacts/outputs/activity_loso_time_fft_branchloss_11_10_8_seed42/window_15_stride_3 \
  --skip-existing \
  --resume
```

---

### 服务器多深度分支 Loss 融合 + Cross-Depth QKV：只跑 `window_15_stride_3`

在前一节三个独立分类 Loss 的基础上，分别在 Time 和 FFT 的三个 Transformer 深度输出之间增加 Cross-Depth QKV。QKV 沿 depth=11/10/8 分支维进行注意力通信，再将相同深度的 Time/FFT 特征配对送入三个分类头。该功能默认关闭，只有显式设置 `--transformer-branch-qkv cross_depth` 才会启用。

```bash
OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12 \
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --device cuda:1 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --transformer-branches 3 \
  --transformer-depths 11 10 8 \
  --transformer-branch-fusion loss_softmax \
  --branch-loss-aux-weight 0.2 \
  --transformer-branch-qkv cross_depth \
  --seed 42 \
  --output-dir local_artifacts/outputs/activity_loso_time_fft_branchloss_crossdepthqkv_11_10_8_seed42/window_15_stride_3 \
  --skip-existing \
  --resume
```

---

## Loss 融合 + Cross-Depth QKV Dropout 参数实验：只跑 `window_15_stride_3`

本实验固定其余配置不变，只调整以下两个参数：

- `--transformer-encoder-dropout`：同时控制每个 `TransformerEncoderBlock` 内的 attention-weight dropout、MHA 输出 dropout、FFN 内部 dropout 和 FFN 输出 dropout；默认值为 `0.5`。
- `--transformer-branch-qkv-dropout`：同时控制 Time/FFT 两侧 `CrossDepthQKVResidual` 的 MHA attention dropout 和残差相加前的输出 dropout；默认值为 `0.1`。

`--input-qkv-dropout` 控制的是 PatchEmbedding 之前的 Input QKV，不是本实验的 Cross-Depth QKV，因此不要用它替代第二个参数。每个概率组合必须写入独立输出目录；以下正式实验命令均使用 `--skip-existing --resume`，可跳过已完成 fold 并续训未完成 fold。

### 1. Smoke test：先确认两个参数均已生效

只跑 subject 1、1 个 epoch，不作为正式结果：

```bash
OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12 \
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --subject-ids 1 \
  --device cuda:1 \
  --epochs 1 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --transformer-branches 3 \
  --transformer-depths 11 10 8 \
  --transformer-encoder-dropout 0.60 \
  --transformer-branch-fusion loss_softmax \
  --branch-loss-aux-weight 0.2 \
  --transformer-branch-qkv cross_depth \
  --transformer-branch-qkv-dropout 0.30 \
  --seed 42 \
  --output-dir local_artifacts/outputs/smoke_branchloss_crossdepthqkv_encdrop_0.60_crossdrop_0.30/window_15_stride_3
```

运行后可检查：

```bash
python - <<'PY'
import json
from pathlib import Path
p = Path("local_artifacts/outputs/smoke_branchloss_crossdepthqkv_encdrop_0.60_crossdrop_0.30/window_15_stride_3/fold_subject_1/metrics.json")
m = json.loads(p.read_text())
print("transformer_encoder_dropout =", m["transformer_encoder_dropout"])
print("transformer_branch_qkv_dropout =", m["transformer_branch_qkv_dropout"])
PY
```

预期分别输出 `0.6` 和 `0.3`。

### 2. 单变量测试 A：提高 Encoder dropout，Cross-Depth 固定为 `0.10`

依次完整运行 Encoder dropout=`0.55/0.60/0.65`：

```bash
set -euo pipefail
export OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12

for encoder_dropout in 0.55 0.60 0.65; do
  ./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
    --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
    --device cuda:1 \
    --epochs 200 \
    --batch-size 72 \
    --class-weights 3,3,1 \
    --input-domain time_fft \
    --conv-type standard \
    --fft-global none \
    --transformer-branches 3 \
    --transformer-depths 11 10 8 \
    --transformer-encoder-dropout "${encoder_dropout}" \
    --transformer-branch-fusion loss_softmax \
    --branch-loss-aux-weight 0.2 \
    --transformer-branch-qkv cross_depth \
    --transformer-branch-qkv-dropout 0.10 \
    --seed 42 \
    --output-dir "local_artifacts/outputs/activity_loso_branchloss_crossdepthqkv_encdrop_${encoder_dropout}_crossdrop_0.10_seed42/window_15_stride_3" \
    --skip-existing \
    --resume
done
```

### 3. 单变量测试 B：Encoder 固定为 `0.50`，提高 Cross-Depth dropout

依次完整运行 Cross-Depth dropout=`0.20/0.30/0.40`：

```bash
set -euo pipefail
export OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12

for cross_depth_dropout in 0.20 0.30 0.40; do
  ./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
    --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
    --device cuda:1 \
    --epochs 200 \
    --batch-size 72 \
    --class-weights 3,3,1 \
    --input-domain time_fft \
    --conv-type standard \
    --fft-global none \
    --transformer-branches 3 \
    --transformer-depths 11 10 8 \
    --transformer-encoder-dropout 0.50 \
    --transformer-branch-fusion loss_softmax \
    --branch-loss-aux-weight 0.2 \
    --transformer-branch-qkv cross_depth \
    --transformer-branch-qkv-dropout "${cross_depth_dropout}" \
    --seed 42 \
    --output-dir "local_artifacts/outputs/activity_loso_branchloss_crossdepthqkv_encdrop_0.50_crossdrop_${cross_depth_dropout}_seed42/window_15_stride_3" \
    --skip-existing \
    --resume
done
```

### 4. 最优概率组合复验模板

完成前两组并分别选出最优值后，修改下面前两行，再运行一次完整 LOSO：

```bash
encoder_dropout=0.60       # 改成单变量测试 A 的最优值
cross_depth_dropout=0.35   # 改成单变量测试 B 的最优值

OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12 \
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --device cuda:1 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --transformer-branches 3 \
  --transformer-depths 11 10 8 \
  --transformer-encoder-dropout "${encoder_dropout}" \
  --transformer-branch-fusion loss_softmax \
  --branch-loss-aux-weight 0.2 \
  --transformer-branch-qkv cross_depth \
  --transformer-branch-qkv-dropout "${cross_depth_dropout}" \
  --seed 42 \
  --output-dir "local_artifacts/outputs/activity_loso_branchloss_crossdepthqkv_best_encdrop_${encoder_dropout}_crossdrop_${cross_depth_dropout}_seed42/window_15_stride_3" \
  --skip-existing \
  --resume
```

### 加入权重
```bash
encoder_dropout=0.85
cross_depth_dropout=0.25
class_weights="20,20,1"
weight_tag="20-20-1"

OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12 \
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights "${class_weights}" \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --transformer-branches 3 \
  --transformer-depths 11 10 8 \
  --transformer-encoder-dropout "${encoder_dropout}" \
  --transformer-branch-fusion loss_softmax \
  --branch-loss-aux-weight 0.2 \
  --transformer-branch-qkv cross_depth \
  --transformer-branch-qkv-dropout "${cross_depth_dropout}" \
  --seed 42 \
  --output-dir "local_artifacts/outputs/activity_loso_branchloss_crossdepthqkv_weight_${weight_tag}_encdrop_${encoder_dropout}_crossdrop_${cross_depth_dropout}_seed42/window_15_stride_3" \
  --skip-existing
```

### 5. 最优组合上的分层三分类（先 e_1/e_2 与 e_3，再 e_1 与 e_2）

新增 `--classification-mode hierarchical`；不传该参数（或传 `flat`）保持原直接三分类。此模式用于 `time_fft` 双域 Conformer，保留原三路深度、cross_depth QKV、分支损失融合及其他训练参数。

每个深度分支使用两个二分类头，联合端到端训练：一级用全部样本区分 `{e_1,e_2}` / `e_3`，二级仅由真实 e_1/e_2 样本监督。每个样本损失为一级交叉熵加上适用时的二级交叉熵；`--class-weights 3,3,1` 对 e_1/e_2/e_3 样本的整条路径损失分别加权 3/3/1。原 `loss_softmax` 和辅助权重 0.2 继续作用于各分支的路径损失。

各分支输出联合三类概率，经可学习权重融合后，先比较 `P(e_1)+P(e_2)` 与 `P(e_3)`；若选中前组，再比较 e_1、e_2。准确率、混淆矩阵和 F1 均使用这个逐级决策；标签仍为 e_1=0、e_2=1、e_3=2。这是两个分类头联合训练，并非先训练完一级再单独训练二级。

使用独立输出目录从头训练新模型。`--resume` 仅续训配置匹配的分层实验，不能接续旧直接三分类检查点；`--skip-existing` 也会检查分类模式。

```bash
encoder_dropout=0.85
cross_depth_dropout=0.25

OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12 \
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --device cuda:1 \
  --epochs 200 \
  --batch-size 72 \
  --class-weights 3,3,1 \
  --classification-mode hierarchical \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --transformer-branches 3 \
  --transformer-depths 11 10 8 \
  --transformer-encoder-dropout "${encoder_dropout}" \
  --transformer-branch-fusion loss_softmax \
  --branch-loss-aux-weight 0.2 \
  --transformer-branch-qkv cross_depth \
  --transformer-branch-qkv-dropout "${cross_depth_dropout}" \
  --seed 42 \
  --output-dir "local_artifacts/outputs/activity_loso_hierarchical_branchloss_crossdepthqkv_best_encdrop_${encoder_dropout}_crossdrop_${cross_depth_dropout}_seed42/window_15_stride_3" \
  --skip-existing \
  --resume
```


### 权重加入
```bash
encoder_dropout=0.85
cross_depth_dropout=0.25
class_weights="3,3,1"
weight_tag="${class_weights//,/-}"

OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12 \
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_loso_batch.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --classification-mode hierarchical \
  --class-weights "${class_weights}" \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --transformer-branches 3 \
  --transformer-depths 11 10 8 \
  --transformer-encoder-dropout "${encoder_dropout}" \
  --transformer-branch-fusion loss_softmax \
  --branch-loss-aux-weight 0.2 \
  --transformer-branch-qkv cross_depth \
  --transformer-branch-qkv-dropout "${cross_depth_dropout}" \
  --seed 42 \
  --output-dir "local_artifacts/outputs/activity_loso_hierarchical_branchloss_crossdepthqkv_weights_applied_v1_${weight_tag}_encdrop_${encoder_dropout}_crossdrop_${cross_depth_dropout}_seed42/window_15_stride_3" \
  --skip-existing \
  --resume
  ```