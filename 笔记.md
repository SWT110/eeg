# 加权交叉商（权重）
## 改脚本：train_activity_loso.py（mac要加 --device mps）
criterion = nn.CrossEntropyLoss().to(device_obj)
改为
class_weights = torch.tensor([3.0, 3.0, 1.0], dtype=torch.float32, device=device_obj)
criterion = nn.CrossEntropyLoss(weight=class_weights).to(device_obj)

## 命令方式
cd /Users/swt/Desktop/eeg
./.conda-envs/eeg-mac-test-train/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device mps \
  --epochs 200 \
  --batch-size 16 \
  --class-weights 3,3,1

cd /Users/swt/Desktop/eeg
./.conda-envs/eeg-mac-test-train/bin/python EEG-Conformer/train_activity_loso_generated_batch.py \
  --device mps \
  --epochs 100 \
  --class-weights 3,3,1