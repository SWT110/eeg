# e_1 / e_2 召回优化：第一轮

新增入口 `EEG-Conformer/train_activity_recall.py` 复用原有双域 Conformer 和分层损失，接受旧 batch 入口的模型、训练、被试、续训参数。旧入口与旧命令的行为不变，旧结果也不会被新流程覆盖。

本轮先固定结构：hierarchical、time_fft、深度 11/10/8、编码器 dropout 0.85、跨深度 dropout 0.25、类别权重 3/3/1。尚未在真实 EEG 上完成训练，不能承诺提高召回或达到 70% 准确率。

## 实验流程

1. 外层 LOSO 留一位被试测试；其余 10 人用固定 split seed 分为 4/3/3 人的三组内层验证。重叠窗口随整个被试分组。
2. 每个内层模型最多训练 `--epochs` 轮，每轮只预测内层验证被试，保存三类 log-probability。标准化统计量只在该内层训练被试上拟合。
3. 在所有「epoch × 决策参数」中，先筛选被试等权平均验证 accuracy ≥ `--min-val-accuracy`（默认 0.70）的候选，再最大化 `min(mean_subject_R1, mean_subject_R2)`。并列按 macro-F1、accuracy、较早 epoch、决策顺序选择。并非先把三个验证组等权平均。
4. 如果所有候选都达不到准确率下限，预先固定回退为最高平均被试 macro-F1，并记录 `accuracy_floor_met=false`。回退并不意味着达到了准确率目标，也不会放宽下限重新搜索。
5. 在全部 10 个外层训练被试上重新初始化模型，固定训练所选轮数。保存检查点与所选门槛之后，再对外层被试预测一次。外层结果不参与选择或重训轮数调整。

分层模式搜索 `P(e_1)+P(e_2)` 门槛（默认 0.35/0.40/0.45/0.50/0.55/0.60），及 `P(e_1 | e_1或e_2)` 门槛（0.45/0.50/0.55）。低于第一层门槛判为 e_3，通过后按第二层门槛区分 e_1/e_2。0.5/0.5 就是旧代码的默认决策，始终包含在候选里。第二层门槛降低会偏向 e_1，提高会偏向 e_2。

`--classification-mode flat` 也支持。此时搜索 `argmax(log_probs + [b1,b2,0])`，b1/b2 默认分别取 0/0.25/0.5，零偏置始终包含。没有在一次训练中混合 flat 与 hierarchical，也没有默认同时搜索类别权重。

每个门槛复用相同的验证输出，不增加训练次数。一个结构、一个权重、一个 seed 的完整 11 人实验需要 33 次内层训练 + 11 次重训；200 轮上限合计 8800 个 epoch。实际 GPU 时间、显存与收敛尚未验证。

## 服务器运行命令

在 eeg 仓库根目录执行。与原命令相比主要是入口改为 `train_activity_recall.py`、使用新目录、增加验证参数；不要把输出目录里的下划线写成星号。

```bash
encoder_dropout=0.85
cross_depth_dropout=0.25
class_weights="3,3,1"
weight_tag="${class_weights//,/-}"

OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12 \
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_recall.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --device cuda:0 \
  --epochs 200 \
  --batch-size 72 \
  --classification-mode hierarchical \
  --class-weights "${class_weights}" \
  --input-domain time_fft \
  --conv-type standard \
  --fft-global none \
  --transformer-branches 3 \
  --transformer-depths 11 10 8 \
  --transformer-encoder-dropout "${encoder_dropout}" \
  --transformer-branch-fusion loss_softmax \
  --branch-loss-aux-weight 0.2 \
  --transformer-branch-qkv cross_depth \
  --transformer-branch-qkv-dropout "${cross_depth_dropout}" \
  --seed 42 \
  --inner-folds 3 \
  --min-val-accuracy 0.70 \
  --cpu-threads 12 \
  --output-dir "local_artifacts/outputs/activity_recall_hierarchical_v1_${weight_tag}_encdrop_${encoder_dropout}_crossdrop_${cross_depth_dropout}_seed42/window_15_stride_3" \
  --skip-existing \
  --resume
```

建议第一次在这条命令末尾加 `--dry-run`，检查 `protocol_plan.json` 的模型、有效权重、划分和训练预算。不构造模型或运行训练。之后去掉 `--dry-run`，保留 `--resume --skip-existing` 即可使用该目录正式运行。

可加 `--subject-ids 1` 先检查一个完整外层折；完成后去掉该参数，已完成折会跳过。不要根据这个被试的外层分数再调参数并当作独立验证。

第一轮先用上述默认网格。后续改变网格示例：

```bash
--gate-thresholds 0.4,0.45,0.5,0.55 \
--within-thresholds 0.45,0.5,0.55
```

如要完全关闭门槛搜索，hierarchical 用 `--gate-thresholds 0.5 --within-thresholds 0.5`，flat 用 `--flat-biases 0`。即使关闭，epoch 仍按新验证规则选择。

后续只改变一个因素：例如同结构权重改成 4,4,1 或 6,6,1，或单独对照 flat，并使用新的输出目录。不要将多次外层试验选出的最高值当作无偏提升；冻结候选协议后再复核多 seed 和新被试。

## 输出与恢复

根目录：

- `protocol_plan.json`：有效参数、数据/代码 SHA-256、运行环境、此次请求的被试划分与预算。
- `summary.json`：该目录已经完成的所有被试的等权平均 accuracy、macro-F1、逐类 recall/precision、合并混淆矩阵，以及未达到内层准确率下限的被试编号。部分折完成时也会更新，查看 `n_subjects`，不要误当作完整 11 人结果。

每个 `fold_subject_N/`：

- `split.json`：外层/内层被试编号。
- `inner_K/effective_training.json`：实际 loss 类型、类别权重、模型参数、归一化统计量及训练/验证被试。
- `inner_K/train_history.json`：训练 loss。
- `inner_K/validation_predictions.npz`：每个 epoch 的验证 `log_probs[epoch,N,3]`、标签、被试及全局样本索引。
- `selection.json`：固定的 epoch/决策参数、所有候选的验证指标、准确率约束是否满足及回退原因。
- `selected_validation_predictions.npz`：所选 epoch 的跨内层验证输出，便于后续分析。
- `refit/`：固定轮数的重训记录与临时/完成产物。
- `refit_checkpoint.pt`：重训权重、模型参数、标准化统计量及部署所需的固定决策参数。
- `outer_predictions.npz`：外层标签、log-probability、原始概率、所选决策与默认决策预测、样本索引。
- `outer_metrics.json`：外层的 `calibrated` 和 `default_decision` 两套指标，最后写入，作为整折完成标记。

两套外层指标来自**同一个按召回选点的重训模型**，只改变决策门槛。这不是两个独立训练/选点的基线模型，不能把差异解释为整个新训练协议相对旧协议的收益。只有一次模型前向评估，从同一组概率计算两套指标。

`--resume` 每个已完成 epoch 恢复模型、Adam、Python/NumPy/Torch/CUDA 随机状态和 DataLoader shuffle 随机状态；中途未提交的 epoch 会重做。已完成内层训练直接读取保存的验证输出。重训阶段也支持逐 epoch 恢复。CPU 上已验证恢复与连续运行一致；跨 GPU/驱动的逐位一致性不作保证。

`--skip-existing` 只跳过本协议、相同配置/数据/代码/软件环境的完整折。修改权重、dropout、epoch 上限、门槛网格、seed 或代码都会拒绝复用目录，必须使用新目录；不会把旧 batch 结果当作本轮结果。`--subject-ids` 仅改变派发范围，可在同目录顺序追加。不要让多个进程同时写同一个输出根目录；并行作业使用各自目录。

每个进行中的训练阶段保存一份模型+优化器检查点，可能明显大于 best_model。内层阶段完成并保存全部验证分数后移除其临时检查点，保留分数用于复核。最后的 refit 权重会保留。

## 使用新检查点预测

新协议的检查点格式与旧 `best_model.pt` 不同，旧预测/API入口没有读取本轮门槛的逻辑。请使用新增的 `predict_activity_recall.py`，它会同时应用训练时的标准化统计量和所选决策：

```bash
./.conda-envs/eegconformer310/bin/python EEG-Conformer/predict_activity_recall.py \
  --checkpoint /path/to/fold_subject_1/refit_checkpoint.pt \
  --input-npy /path/to/new_windows.npy \
  --output-npz /path/to/new_predictions.npz \
  --device cuda:0 --batch-size 72
```

输入必须是与训练 `X.npy` 上游预处理一致的 `[N,21,1920]` 窗口，不能直接提供原始 EDF。预测脚本不会利用新数据重估标准化或调整门槛。LOSO 检查点是各自折的模型，本轮没有额外训练全被试部署模型。

## 解释结果的边界

现有 X.npy 在切窗前已经按完整视频记录归一化，且窗口、结构等曾在这 11 人上探索。本协议隔离后续选点，不会把这批数据变成独立确认集。内层模型门槛迁移到重新训练的模型也不保证同样有效；外层同时报告默认决策就是为了检查这一点。不要用外层标签重新选门槛。

先看每类召回是否同时改善、accuracy/precision 的损失是否可接受，再看 S6、S2、S3 的逐被试结果。回传 `protocol_plan.json`、`summary.json`、各折 `selection.json` 与 `outer_metrics.json` 即可做下一轮诊断；需要画门槛曲线时再使用预测 NPZ。

## 本次实际验证

- 76 个相关测试通过：新协议 10 项、旧 batch 命令兼容、原分层模型与原被试验证测试。覆盖类别权重对实际损失及梯度的影响、按被试等权的约束/回退选点、外层标签不影响选点和重训权重、内层及重训的断点恢复、配置不匹配拒绝复用、预测时应用保存门槛。
- 合成数据的真实双域分层模型通过命令行完成 2 个内层训练及 1 次重训/外层评估；独立预测命令得到的留出样本分数和类别，与训练输出逐项一致。
- 原始尺寸 `1×1×21×1920` 时间输入与 `1×1×21×961` FFT 输入，深度 11/10/8、dropout 0.85/0.25 的模型完成 CPU 前向，输出三类概率和为 1，参数量 12,340,561。这是形状检查，不是训练效果验证。
- 真实 5765 窗口、11 人数据集已生成完整 dry-run 计划，确认有效权重 3/3/1、33 次内层训练和 11 次重训。没有运行真实 EEG 全量训练或验证 CUDA 性能。

测试命令（在 eeg 仓库根目录，Python 指向已安装依赖的环境）：

```bash
python -m pytest EEG-Conformer/tests/test_train_activity_recall.py EEG-Conformer/tests/test_train_activity_loso_batch.py EEG-Conformer/tests/test_activity_hierarchical.py EEG-Conformer/tests/test_subject_validation.py -q
```
