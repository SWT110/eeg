# e_1 / e_2 三倍重采样：三组对照实验方案

状态：研究者已确认方案；独立训练入口和 seed 42 命令已准备，尚未在云 GPU 上完成训练。

## 研究问题

在固定 EEG 视频刺激条件分类模型、数据集和验证规则的情况下，比较训练窗口有放回重采样与无重采样、类别加权两种训练设置对 `e_1`、`e_2` 召回率的影响。预测目标是受试者正在观看的三段指定视频，不据此推断独立的认知状态。

主数据集为 `window_15_stride_3`。完整数据的类别窗口数为 `e_1=1056`、`e_2=1122`、`e_3=3587`；多数类窗口较多主要源于第三段视频的记录时长较长。相邻窗口有 12 秒重叠，重复抽取不增加独立记录或受试者。

## 预先固定的三组

| 组别 | 每轮训练索引 | 损失类别权重 |
|---|---|---|
| A：无平衡处理 | 每个训练窗口一次，随机打乱 | `1,1,1` |
| B：仅类别加权 | 每个训练窗口一次，随机打乱 | `3,3,1` |
| C：仅三倍重采样 | `e_3` 每个训练窗口一次；分别从训练集的 `e_1`、`e_2` 窗口有放回地抽取各自原数量的三倍；汇总后随机打乱 | `1,1,1` |

每轮 C 组的类别抽取总数固定，但重复或未抽中的具体 `e_1`、`e_2` 窗口随随机数变化。抽样只在当次内层训练被试或外层重训被试中进行；内层验证和外层测试保留原始窗口及类别比例。

三组均沿用 `召回率优化训练说明.md` 中的同一分层 `time_fft` 模型、`11/10/8` 深度、编码器 dropout `0.85`、跨深度 dropout `0.25`，以及相同的训练参数、数据预处理与决策门槛候选。除训练索引抽样和损失权重外，不改变网络结构。

## 划分、选点和报告

1. 对 11 名受试者做外层 LOSO。每个外层折只将其余 10 人用于训练和内层验证；重叠窗口始终随受试者一起划分。标准化统计量只由对应训练被试拟合。
2. 每组、每个内层训练任务的上限均为 200 个 epoch，batch size 为 72。用内层验证被试的预测在 `epoch × 决策门槛` 中选择操作点：先筛选被试等权平均 accuracy 不低于 `0.70` 的候选，再最大化 `min(mean_subject_R1, mean_subject_R2)`；按现有协议处理并列及无合格候选的回退。
3. 按所选 epoch 在全部外层训练被试上重训该组模型；冻结决策门槛后，对留出的外层测试被试评估一次。三组都按这一规则选择各自的最佳 epoch，外层测试标签不参与选择。
4. 首轮三组均运行 seed `42`，完整报告 11 人的被试等权 `R1/R2/R3`、precision、accuracy、macro-F1、混淆矩阵及每名被试的结果。后续是否追加 seed `43/44` 再作决定；在追加前，seed `42` 仅支持探索性结论，不声称跨种子稳定提升。

## 解释边界

- C 组保持与 A/B 相同的 epoch 上限，但每轮约有 `3n_1+3n_2+n_3` 次窗口呈现，A/B 为 `n_1+n_2+n_3`。按完整数据计是 `10121/5765≈1.76` 倍；实际每个折按其训练被试数量计算。因此三组差异同时包含抽样分布与参数更新次数的影响，不能单独归因于其中一项。
- 以前的 `[3,3,1]` 结果可作为历史参考，但旧流程以外层测试准确率选择最佳 epoch；以前的无权重结果使用另一网络结构。两者不能代替本方案的 A/B 组结果，也无需推翻或删除。
- 不根据 seed `42` 的外层结果事后改变本轮三组、模型或门槛规则后仍声称是预先固定的比较。若将来要作稳健性结论，三组按一致的追加种子及协议评估，并如实说明追加种子的决定时间。

## 与原实验的兼容性

本次仅新增 `EEG-Conformer/train_activity_resampling.py` 和 `EEG-Conformer/run_recall_resampling_seed42.sh`。以前的 `train_activity_loso.py`、`train_activity_loso_batch.py`、`train_activity_recall.py` 及其原有训练命令不改动。三组新结果各写入独立目录，不覆盖以前的输出。

## 本地更新 GitHub

在本机 `eeg` 仓库根目录执行，先检查变更，再只提交本次代码与文档：

```bash
cd /Volumes/S70/Desktop/lunwen/eeg
git status --short
git add CONTEXT.md 三倍重采样三组对照实验方案.md \
  EEG-Conformer/train_activity_resampling.py \
  EEG-Conformer/run_recall_resampling_seed42.sh \
  EEG-Conformer/tests/test_train_activity_resampling.py
git commit -m "Add isolated three-arm EEG recall resampling experiment"
git push origin main
```

`local_artifacts/` 中的 EEG 数据与训练输出被 Git 忽略，云服务器拉取仓库不会自动获得这些数据。服务器上需已有相同的 `window_15_stride_3` 数据集。

## 云 GPU 服务器拉取与训练

以下命令从云服务器的 `eeg` 仓库根目录执行。脚本默认使用 `./.conda-envs/eegconformer310/bin/python`、`cuda:0`、seed `42`；若环境路径不同，设置 `PYTHON_BIN`。先拉取代码并检查数据：

```bash
cd /path/to/eeg
git pull --ff-only origin main
test -f local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3/X.npy
test -x .conda-envs/eegconformer310/bin/python
```

先生成三组计划，不训练：

```bash
RUN_MODE=dry-run ./EEG-Conformer/run_recall_resampling_seed42.sh
```

核对三份 `protocol_plan.json` 的模型、被试划分、类别权重和 `train_sampling` 后，启动 seed 42 的三组完整 LOSO：

```bash
RUN_MODE=train ./EEG-Conformer/run_recall_resampling_seed42.sh
```

脚本顺序运行 `unweighted`、`weighted_3_3_1`、`triple_minority`；中断后重复相同命令会从已完成的 epoch 或折恢复。若需单独启动一组，可用：

```bash
RUN_GROUPS=triple_minority RUN_MODE=train ./EEG-Conformer/run_recall_resampling_seed42.sh
```

三组结果分别保存在 `local_artifacts/outputs/activity_recall_resampling_seed42/<组名>/window_15_stride_3/`。每组完整结束后，`summary.json` 的 `n_subjects` 应为 `11`；未完成 11 折的摘要不能作为最终结果。脚本固定相同的模型与 200 epoch 上限，只让类别权重、训练采样方式和各自选择的最佳 epoch 随组别变化。

也可以直接运行采样组入口，便于照 `笔记.md` 风格修改命令：

```bash
OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12 \
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_resampling.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --output-dir local_artifacts/outputs/activity_recall_resampling_seed42/triple_minority/window_15_stride_3 \
  --device cuda:0 --epochs 200 --batch-size 72 \
  --classification-mode hierarchical --class-weights 1,1,1 \
  --train-sampling triple_minority_replacement \
  --input-domain time_fft --conv-type standard --fft-global none \
  --transformer-branches 3 --transformer-depths 11 10 8 \
  --transformer-encoder-dropout 0.85 \
  --transformer-branch-fusion loss_softmax --branch-loss-aux-weight 0.2 \
  --transformer-branch-qkv cross_depth --transformer-branch-qkv-dropout 0.25 \
  --seed 42 --inner-folds 3 --min-val-accuracy 0.70 --cpu-threads 12 \
  --skip-existing --resume
```
