# e_1 / e_2 三倍重采样：三组历史口径对照实验

状态：按研究者最新要求，三组统一采用每折最高测试准确率选 epoch 的历史口径；独立训练入口和 seed 42 命令已准备，尚未在云 GPU 上完成训练。

## 研究问题

在固定 EEG 视频刺激条件分类模型、数据集和训练轮数的情况下，比较无平衡处理、类别加权与三倍有放回重采样三组的每折最高测试准确率，以及该 epoch 对应的混淆矩阵和 `e_1`、`e_2` 召回率。预测目标是受试者正在观看的三段指定视频，不据此推断独立的认知状态。

主数据集为 `window_15_stride_3`。完整数据的类别窗口数为 `e_1=1056`、`e_2=1122`、`e_3=3587`；多数类窗口较多主要源于第三段视频的记录时长较长。相邻窗口有 12 秒重叠，重复抽取不增加独立记录或受试者。

## 预先固定的三组

| 组别 | 每轮训练索引 | 损失类别权重 |
|---|---|---|
| A：无平衡处理 | 每个训练窗口一次，随机打乱 | `1,1,1` |
| B：仅类别加权 | 每个训练窗口一次，随机打乱 | `3,3,1` |
| C：仅三倍重采样 | `e_3` 每个训练窗口一次；分别从训练集的 `e_1`、`e_2` 窗口有放回地抽取各自原数量的三倍；汇总后随机打乱 | `1,1,1` |

每轮 C 组的类别抽取总数固定，但重复或未抽中的具体 `e_1`、`e_2` 窗口随随机数变化。抽样只在当折的 10 名训练被试中进行；留出的测试被试保留原始窗口及类别比例。

三组均使用同一分层 `time_fft` 模型、`11/10/8` 深度、编码器 dropout `0.85`、跨深度 dropout `0.25`，以及相同的训练参数和数据预处理。预测使用原模型的固定决策规则，不搜索额外门槛。除训练索引抽样和损失权重外，不改变网络结构。

## 划分、选点和报告

1. 对 11 名受试者做 LOSO：每折用 10 人训练，留出 1 人测试。重叠窗口随整个受试者划分；标准化统计量只由 10 名训练被试拟合。
2. 三组每折均最多训练 200 个 epoch，batch size 为 72。每个 epoch 都预测该折的测试被试，按**最高测试准确率**选该折的最佳 epoch；完全相同的准确率优先较早 epoch。这里不使用 `--min-val-accuracy`，也不做内层验证或决策门槛搜索。
3. 各组分别取 11 个折的最佳测试准确率，计算算术平均；将每折最佳 epoch 的预测合并为总体混淆矩阵，同时报告每折及 11 人等权平均的逐类召回率、precision 和 macro-F1。`summary.json` 只有在 `n_subjects=11` 时才是完整结果。
4. 首轮三组均运行 seed `42`。后续是否追加 seed `43/44` 再作决定；在追加前，seed `42` 不支持跨种子稳定性结论。

## 解释边界

- 每折用测试被试选最佳 epoch，故“平均最高测试准确率”是与旧实验一致的描述性指标，**高估独立未见被试的泛化准确率**；同一批测试结果不能再作为无偏验证。论文应明确报告这一选择方式。
- C 组保持与 A/B 相同的 epoch 上限，但每轮约有 `3n_1+3n_2+n_3` 次窗口呈现，A/B 为 `n_1+n_2+n_3`。按完整数据计是 `10121/5765≈1.76` 倍；实际每个折按其训练被试数量计算。因此三组差异同时包含抽样分布与参数更新次数的影响，不能单独归因于其中一项。
- 以前的 `[3,3,1]` 结果虽已完成 11 折，但其 `metrics.json` 记录的是 `LearnableSoftmaxWeightedBranchCrossEntropyLoss`（旧 flat 分类）；本方案三组统一使用 `hierarchical` 分类及对应 NLL 损失，故旧加权结果不能代替 B 组。以前的无权重结果也使用另一网络结构，不能代替 A 组。三组均需在本方案中运行；旧输出保留作为历史参考。
- 若将来要作稳健性结论，三组按一致的追加种子及协议评估，并如实说明追加种子的决定时间。

## 与原实验的兼容性

本次仅修改独立的 `EEG-Conformer/train_activity_resampling.py` 和 `EEG-Conformer/run_recall_resampling_seed42.sh`。以前的 `train_activity_loso.py`、`train_activity_loso_batch.py`、`train_activity_recall.py` 及其原有训练命令不改动。三组新结果写入 `activity_resampling_test_best_seed42`，不覆盖此前的 `activity_recall_resampling_seed42` 输出。

## 本地更新 GitHub

在本机 `eeg` 仓库根目录执行，先检查变更，再提交这次口径修订：

```bash
cd /Volumes/S70/Desktop/lunwen/eeg
git status --short
git add 三倍重采样三组对照实验方案.md \
  EEG-Conformer/train_activity_resampling.py \
  EEG-Conformer/run_recall_resampling_seed42.sh \
  EEG-Conformer/tests/test_train_activity_resampling.py
git commit -m "Align resampling comparison with best test epoch protocol"
git push origin main
```

`local_artifacts/` 中的 EEG 数据与训练输出被 Git 忽略，云服务器拉取仓库不会自动获得这些数据。服务器上需已有相同的 `window_15_stride_3` 数据集。

## 云 GPU 服务器拉取与训练

以下命令从云服务器的 `eeg` 仓库根目录执行。脚本默认使用 `./.conda-envs/eegconformer310/bin/python`、`cuda:0`、seed `42`；若环境路径不同，设置 `PYTHON_BIN`。先拉取代码并检查数据：

```bash
cd /path/to/eeg
git pull --ff-only origin main
test -f local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3/X.npy
test -x .conda-envs/eegconformer310/bin/python
```

先生成三组计划，不训练：

```bash
RUN_MODE=dry-run ./EEG-Conformer/run_recall_resampling_seed42.sh
```

核对三份 `protocol_plan.json` 的模型、测试被试、类别权重、`train_sampling` 和 `selection` 后，启动 seed 42 的三组完整 LOSO：

```bash
RUN_MODE=train ./EEG-Conformer/run_recall_resampling_seed42.sh
```

脚本顺序运行 `unweighted`、`weighted_3_3_1`、`triple_minority`；中断后重复相同命令会从已完成的 epoch 或折恢复。若需单独启动一组，可用：

```bash
RUN_GROUPS=triple_minority RUN_MODE=train ./EEG-Conformer/run_recall_resampling_seed42.sh
```

三组结果分别保存在 `local_artifacts/outputs/activity_resampling_test_best_seed42/<组名>/window_15_stride_3/`。每组完整结束后，`summary.json` 的 `n_subjects` 应为 `11`；未完成 11 折的摘要不能作为最终结果。`mean_best_test_acc` 是 11 折各自最高测试准确率的平均，`pooled_confusion_matrix` 合并各折最佳 epoch 的混淆矩阵。

也可以直接运行采样组入口，便于照 `笔记.md` 风格修改命令：

```bash
OMP_NUM_THREADS=12 MKL_NUM_THREADS=12 OPENBLAS_NUM_THREADS=12 NUMEXPR_NUM_THREADS=12 \
./.conda-envs/eegconformer310/bin/python EEG-Conformer/train_activity_resampling.py \
  --dataset-root local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --output-dir local_artifacts/outputs/activity_resampling_test_best_seed42/triple_minority/window_15_stride_3 \
  --device cuda:0 --epochs 200 --batch-size 72 \
  --classification-mode hierarchical --class-weights 1,1,1 \
  --train-sampling triple_minority_replacement \
  --input-domain time_fft --conv-type standard --fft-global none \
  --transformer-branches 3 --transformer-depths 11 10 8 \
  --transformer-encoder-dropout 0.85 \
  --transformer-branch-fusion loss_softmax --branch-loss-aux-weight 0.2 \
  --transformer-branch-qkv cross_depth --transformer-branch-qkv-dropout 0.25 \
  --seed 42 --cpu-threads 12 \
  --skip-existing --resume
```
