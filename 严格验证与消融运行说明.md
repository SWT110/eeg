# 严格验证与消融：第一轮代码交付

本次新增独立入口 `EEG-Conformer/train_activity_subject_validation.py` 和配置 `EEG-Conformer/configs/subject_validation_first_round.json`，没有改写旧训练入口，也没有进行真实数据集训练。已经完成合成数据 CPU 测试、12 个模型预设的单次前向检查，以及真实 15 秒窗口／3 秒步长数据集的计划生成。

## 评估规则

1. 外层按被试留一。每个外层折只使用其余被试进行模型训练和选择。
2. 将外层训练被试按固定 `split_seed=20260906` 打乱，分成 3 个互斥的验证组；每位外层训练被试恰好进入一个验证组。同一被试的全部重叠窗口一起分组。10 位训练被试对应 4／3／3 人三个验证组。
3. 每个候选配置在每个内层训练组上独立训练，默认最多 200 个 epoch。每个 epoch 计算验证组内每位被试的三类 macro-F1，再对所有验证被试等权平均。不会先把 4／3／3 人三组等权平均，也不会把所有窗口合并后用于选点。
4. 在“候选配置 × epoch”中选择最高平均被试 macro-F1。浮点分数完全相同时，先选较早 epoch，再选配置文件中较早的候选项。三类均参与 macro-F1，零分母记为 0。
5. 重新初始化模型和优化器，在全部外层训练被试上训练所选的固定 epoch 数。内层权重不延续到这一步。
6. 固定重训完成、检查点和选择结果保存后，只对外层被试进行一次预测和评估。没有逐 epoch 的外层测试指标，也不根据外层结果改变 epoch 或候选。

训练随机种子默认为 42／43／44。内层训练种子为 `seed + 1009 × (内层折编号 + 1)`，重训种子为 `seed + 1000003`。同一内层折的候选共享初始化种子；模型不同会消耗不同随机数，因此不承诺不同结构的参数逐一配对。启用 PyTorch 确定性算法，不支持的非确定性操作会直接报错；跨硬件或 PyTorch 版本不保证逐位一致。

时间域和 log-power rFFT 域各自使用训练样本元素的一个标量均值和总体标准差。每个内层训练组各拟合一组统计量，最终重训阶段重新使用全部外层训练被试拟合统计量。验证和测试数据仅应用对应训练统计量。

**这仍然是现有数据上的内部复核。** 当前 `X.npy` 在切窗前已经按完整视频记录逐通道标准化，新增入口无法撤销这种对被试记录分布的适应。窗口、深度、dropout、类别权重等也曾在同一批 11 人上筛选。新入口隔离后续的选点与候选选择，但不能把历史探索过的数据变成独立确认集；论文不得据此称为独立确认性结果。

## 已准备的模型与消融

| 预设名称 | 设置与解释 |
|---|---|
| `time` | 时间域 Conformer，深度 6，编码器 dropout 0.5。 |
| `time_fft` | 时间域＋FFT 双分支，深度 6，编码器 dropout 0.5。 |
| `mdtf` | 深度 11／10／8，CD-QKV，可学习分支融合，辅助系数 0.2。 |
| `no_cross_depth` | 在 MDTF 设置上移除 CD-QKV。 |
| `uniform_fusion` | 在 MDTF 设置上将分支权重 logits 固定为零，并关闭其梯度，使预测融合与分支损失权重均为 1／3。 |
| `no_aux` | 在 MDTF 设置上将额外平均分支损失系数设为 0；加权分支损失仍保留。 |
| `repeated_depth` | 每个域用三个独立的深度 10 编码器。总计每域 30 层，相比 11＋10＋8 的 29 层，参数量相近但不完全相等。 |
| `singledepth11` | 双域单深度 11，编码器 dropout 0.85，没有跨深度交互。 |
| `eegnet`／`shallowconvnet`／`atcnet`／`tcformer` | 重用本项目已有外部模型实现，输入为时间域，结构配置保持实现默认值。 |

`mdtf`、`no_cross_depth`、`uniform_fusion`、`no_aux`、`repeated_depth` 统一固定编码器 dropout 0.85、CD-QKV dropout 0.25，其他结构保持一致。现有模型构造函数及损失函数被复用，旧的测试选点训练函数没有被调用。

配置默认只有 `fixed_prior` 一个候选：Adam 学习率 0.0002、系数 0.5／0.999、类别权重 3／3／1、batch size 72。这些是先前开发设置，配置中已标注来源，因此默认只在内层选择 epoch 数。

需要扩大搜索时，在第一次外层训练前冻结一份新配置，在 `hyperparameter_candidates` 中增加不同的 `learning_rate` 或 `class_weights`。`class_weights: null` 表示不加类别权重。例如可以预设等权损失与 3／3／1 两个候选。所有受控变体使用同一候选列表。架构由预设锁定，未知候选参数会报错，避免拼写错误被静默忽略。当前没有为每个外部基线实现独立架构搜索，共享搜索次数不等于已经对各模型充分调优。

## 运行方法

以下 PowerShell 命令从项目根目录 `C:\Users\study\Desktop\lunwen` 执行。用于计划及测试的本地环境是 `.venv-comparison`，安装的是 CPU 版 PyTorch，不能据此运行 CUDA 训练。

只生成计划，不构造模型、不训练：

```powershell
& .\eeg\.venv-comparison\Scripts\python.exe .\eeg\EEG-Conformer\train_activity_subject_validation.py --dataset-root .\eeg\local_artifacts\data_to_list\global_activity_dataset\window_15_stride_3 --output-dir .\eeg\local_artifacts\outputs\subject_validation_plan_new --dry-run
```

默认完整计划包含 12 个模型、11 个外层被试、3 个种子，共 396 次外层重训与 1188 次内层训练，上限 316800 个训练 epoch。它不是一次低成本训练；应先在 GPU 环境按模型／被试／种子分批派发，并评估实际耗时和显存。本轮没有运行这些训练。

下面是 GPU 环境中的单个“MDTF／被试 1／种子 42”派发示例；`python` 应指向已安装 CUDA 版 PyTorch 的环境，输出目录必须是新的：

```powershell
python .\eeg\EEG-Conformer\train_activity_subject_validation.py --dataset-root .\eeg\local_artifacts\data_to_list\global_activity_dataset\window_15_stride_3 --output-dir .\eeg\local_artifacts\outputs\subject_validation_mdtf_s01_seed42 --models mdtf --test-subject-ids 1 --seeds 42 --device cuda:0
```

多个值用空格分隔，例如 `--models mdtf no_cross_depth uniform_fusion no_aux repeated_depth --test-subject-ids 1 2 --seeds 42 43 44`。省略三个筛选参数时使用完整配置。每个作业必须使用自己的输出目录，不要让多个进程共享同一运行目录。

任何已有输出目录都会被拒绝覆盖，包括计划目录。当前没有断点续训功能；中断时会保留已完成的内层评分和外层结果，补跑应使用新目录及缺失的被试／种子范围。不要把同一折的多个完成版本按外层分数再选优。默认保存每个外层模型检查点，完整 396 折运行需要数 GB 的输出空间；输入处理也需要足够的主机内存。

## 输出与复核

运行目录保存 `protocol_plan.json`：有效配置、分组、候选规则、种子公式、数据和代码 SHA-256、Python／NumPy／PyTorch／CUDA 版本、任务预算和状态。数据哈希覆盖 `X.npy`、`y.npy`、`subject_ids.npy`、`metadata.json`；真实数据以只读内存映射加载，计划生成也会流式读取文件计算哈希。

每个 `模型/seed_种子/subject_被试/` 子目录保存：

- `split.json`：被试划分。
- `inner_scores.json`：每候选、每 epoch、每验证被试的指标及对应训练损失。
- `selection.json`：所选候选与固定 epoch 数，及全部候选／epoch 的聚合分数。
- `normalizers.json`：每个内层组和外层重训的训练统计量。
- `refit_train_history.json`：最终重训的训练损失，不包含外层逐 epoch 指标。
- `refit_checkpoint.pt`：最终固定 epoch 模型、构建参数和标准化统计量。
- `outer_metrics.json`：一次外层评估的 accuracy、macro-F1、balanced accuracy、混淆矩阵、各类指标和参数量。
- `outer_predictions.npz`：真实／预测标签、原始样本索引及被试编号。

运行根目录的 `completed_outer_folds.json` 汇集已完成外层折。最终报告应先按种子分别做被试等权汇总，再说明种子波动；3 个种子并不把 11 位被试变成 33 个独立样本。不直接混用旧入口的 `best_acc` 摘要与此入口的单次外层结果。

## 本轮实际完成的验证

2026-09-06，本地 `torch 2.14.0+cpu` 环境完成：

- 6 个协议测试全部通过，覆盖被试隔离、训练集统计量、按被试选点与并列规则、固定均匀融合、一次微型合成数据完整内层训练及外层重训、外层只评估一次、禁止覆盖、dry-run 不建模训练。
- 12 个预设分别在 `1 × 1 × 21 × 1920` 时间输入、需要时配 `1 × 1 × 21 × 961` FFT 输入上完成一次 CPU 前向，输出均为三类 logits。这是形状检查，没有训练这些真实尺寸模型。
- 真实 `window_15_stride_3` 数据（5765 × 21 × 1920，11 人）完成完整计划生成，保存在 `local_artifacts/outputs/subject_validation_plan_20260906_final/protocol_plan.json`；未产生真实数据训练结果。

测试复现命令（测试临时目录应选项目内新的目录，避免系统临时目录权限限制）：

```powershell
& .\eeg\.venv-comparison\Scripts\python.exe -m pytest .\eeg\EEG-Conformer\tests\test_subject_validation.py -q --basetemp .\eeg\local_artifacts\outputs\subject_validation_cpu_tests_new
```

本轮没有验证 GPU 运行、完整训练收敛、独立被试性能或改善后的准确率。新增脚本是下一轮实验的可运行准备，不能当作实验已完成的证据。
