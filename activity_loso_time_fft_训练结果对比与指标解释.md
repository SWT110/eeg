# activity_loso_time_fft 训练结果对比与指标解释

本文档汇总 `local_artifacts/outputs/activity_loso_time_fft` 下各个 time+fft 双分支训练配置的 LOSO 实验结果，并加入 time 单分支、fft 单分支结果作为对比，同时解释表格中各指标的含义和作用。

## 1. 各训练配置总体结果对比

按 `mean best acc` 从高到低排序。表中 `time+fft双分支` 来自 `activity_loso_time_fft`，`time单分支` 和 `fft单分支` 作为对照组加入。

| 排名 | 类型 | 子实验 | folds | train/test样本 | mean best acc | std | macro F1 | mean avg acc | 最好被试 | 最差被试 | 平均 best epoch | best-test时 train acc | final train acc |
|---:|---|---|---:|---|---:|---:|---:|---:|---|---|---:|---:|---:|
| 1 | time+fft双分支 | `window_15_stride_3` | 11 | 5239 / 526 | **0.6855** | 0.0612 | **0.5559** | **0.5131** | S4 0.7738 | S2 0.5494 | 78.3 | 0.4702 | 0.9908 |
| 2 | time+fft双分支 | `window_9_stride_9` | 11 | 1773 / 178 | 0.6812 | 0.0511 | 0.5332 | 0.4809 | S4 0.7584 | S9 0.6124 | 54.1 | 0.3920 | 0.9807 |
| 3 | time单分支(3:3:1) | `window_3_stride_3` | 11 | 5359 / 538 | 0.6790 | 0.0625 | 0.4999 | 0.4435 | S4 0.8030 | S8 0.6059 | 85.0 | 0.4174 | 0.8913 |
| 4 | time单分支(1:1:1) | `window_3_stride_3` | 11 | 5359 / 538 | 0.6692 | 0.0538 | 0.4838 | 0.4716 | S4 0.7937 | S6 0.6190 | 97.3 | 0.6101 | 0.9102 |
| 5 | time+fft双分支 | `window_15_stride_15` | 11 | 1066 / 107 | 0.6615 | 0.0509 | 0.4801 | 0.4843 | S4 0.8037 | S11 0.6168 | 63.3 | 0.3734 | 0.9870 |
| 6 | time+fft双分支 | `window_9_stride_3` | 11 | 5299 / 532 | 0.6522 | 0.0717 | 0.5047 | 0.4765 | S4 0.7726 | S2 0.4812 | 39.8 | 0.4418 | 0.9815 |
| 7 | time+fft双分支 | `window_3_stride_3` | 11 | 5359 / 538 | 0.6459 | **0.0504** | 0.4311 | 0.4110 | S4 0.7844 | S3 0.5899 | 75.2 | 0.4142 | 0.9221 |
| 8 | time+fft双分支 | `window_9_stride_1` | 11 | 15857 / 1592 | 0.6457 | 0.0762 | 0.4907 | 0.4489 | S4 0.7337 | S2 0.4661 | 34.5 | 0.5240 | 0.9917 |
| 9 | time+fft双分支 | `window_3_stride_1` | 11 | 16037 / 1610 | 0.6255 | 0.1022 | 0.4252 | 0.3497 | S4 0.8025 | S2 0.3578 | 48.2 | 0.4506 | 0.9750 |
| 10 | fft单分支 | `window_3_stride_3` | 11 | 5359 / 538 | 0.5875 | 0.1292 | 0.4229 | 0.2634 | S6 0.7435 | S2 0.3030 | 27.9 | 0.3898 | 0.7832 |

## 2. 各被试 best test acc 对比

| subject | time 1:1:1 3/3 | time 3:3:1 3/3 | fft 3/3 | time+fft 3/1 | time+fft 3/3 | time+fft 9/1 | time+fft 9/3 | time+fft 9/9 | time+fft 15/3 | time+fft 15/15 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 0.7268 | 0.7454 | 0.6784 | 0.6534 | 0.6190 | 0.6727 | 0.7030 | **0.7528** | 0.6768 | 0.6449 |
| 2 | 0.6357 | 0.6283 | 0.3030 | 0.3578 | 0.6320 | 0.4661 | 0.4812 | 0.6180 | 0.5494 | 0.6168 |
| 3 | 0.6480 | 0.6847 | 0.6132 | 0.6225 | 0.5899 | **0.7201** | 0.6027 | 0.6725 | 0.6218 | 0.6505 |
| 4 | 0.7937 | 0.8030 | 0.6059 | 0.8025 | 0.7844 | 0.7337 | 0.7726 | 0.7584 | 0.7738 | **0.8037** |
| 5 | **0.6729** | **0.6729** | 0.6171 | 0.6155 | 0.6245 | 0.6683 | 0.6560 | 0.6685 | 0.6597 | **0.6729** |
| 6 | 0.6190 | 0.6245 | **0.7435** | 0.6286 | 0.6320 | 0.6351 | 0.6466 | 0.6685 | 0.6483 | 0.6729 |
| 7 | 0.7212 | **0.7602** | 0.7361 | 0.7255 | 0.6933 | 0.6709 | 0.6635 | 0.6910 | 0.6901 | 0.6355 |
| 8 | 0.6264 | 0.6059 | 0.6190 | 0.6168 | 0.6301 | 0.6432 | 0.6429 | 0.6854 | **0.7167** | 0.6542 |
| 9 | 0.6190 | 0.6190 | 0.5149 | 0.6000 | 0.6134 | 0.6244 | 0.6090 | 0.6124 | **0.7338** | 0.6168 |
| 10 | 0.6264 | 0.6357 | 0.6375 | 0.6391 | 0.6580 | 0.7230 | 0.7256 | 0.7472 | **0.7510** | 0.6916 |
| 11 | 0.6729 | 0.6896 | 0.3941 | 0.6193 | 0.6283 | 0.5459 | 0.6711 | 0.6180 | **0.7186** | 0.6168 |

## 3. 参数解释表

| 参数 | 是什么 | 作用 |
|---|---|---|
| `mean best acc` | 所有 LOSO fold 的最高测试准确率平均值 | 判断一个实验配置的总体最好泛化性能，越高越好 |
| `std` | 各 fold 的 `best acc` 标准差 | 判断不同被试之间表现是否稳定，越低越稳定 |
| `macro F1` | 各类别 F1 分数的平均值 | 判断三分类中每一类是否都识别得好，适合类别不均衡情况，越高越好 |
| `mean avg acc` | 各 fold 在所有 epoch 上测试准确率平均值的再平均 | 判断训练过程中整体测试表现是否稳定，不只看某一轮最高值 |
| `最好被试` | 测试准确率最高的那个被试 fold | 看哪个被试最容易泛化识别 |
| `最差被试` | 测试准确率最低的那个被试 fold | 找出模型最难泛化的被试，便于分析问题 |
| `平均 best epoch` | 各 fold 达到最高测试准确率时的 epoch 平均值 | 判断模型大概训练到第几轮效果最好，可辅助选择训练轮数和 early stopping |
| `best-test时 train acc` | 在测试准确率最高的那个 epoch，对应的训练准确率 | 判断模型在最佳测试点时训练集拟合程度 |
| `final train acc` | 最后一个 epoch 的训练准确率 | 判断模型最终对训练集拟合到什么程度，常用于观察是否过拟合 |

## 4. 各参数详细解释

### 4.1 mean best acc

每个 LOSO fold 都会留一个被试作为测试集，其余被试作为训练集。每个 fold 训练 200 个 epoch 后，会记录该 fold 训练过程中最高的测试准确率，即 `best_test_acc`。

`mean best acc` 就是所有被试 fold 的 `best_test_acc` 平均值。

它回答的问题是：

> 这个配置在所有被试上的总体最高测试表现怎么样？

这是选择最终模型时最重要的指标之一。

### 4.2 std

`std` 是标准差，用来衡量不同被试之间的结果波动。

如果两个实验：

```text
A: mean best acc = 0.68, std = 0.05
B: mean best acc = 0.68, std = 0.12
```

虽然平均准确率一样，但 A 更稳定。B 说明有些被试效果很好，有些被试效果很差。

它回答的问题是：

> 这个实验对不同被试是否稳定？

### 4.3 macro F1

当前任务是三分类，而且类别样本可能不均衡。如果某一类样本很多，模型只偏向预测大类，也可能得到不错的 accuracy。

`macro F1` 是先分别计算每个类别的 F1：

```text
class 0 F1
class 1 F1
class 2 F1
```

然后直接平均：

```text
macro F1 = (F1_0 + F1_1 + F1_2) / 3
```

它不按类别样本数加权，因此每个类别的重要性相同。

它回答的问题是：

> 模型是不是对每个类别都识别得比较均衡？

如果 accuracy 高但 macro F1 低，说明模型可能偏向多数类。

### 4.4 mean avg acc

`best acc` 只取训练过程中最高的一次测试准确率。`avg acc` 则是整个训练过程中所有 epoch 的测试准确率平均值。

例如训练 200 个 epoch：

```text
最高 test acc = 0.75
平均 test acc = 0.50
```

说明模型虽然某一轮达到过 0.75，但整体训练过程并不稳定。

它回答的问题是：

> 模型整个训练过程中的测试表现稳定不稳定？

### 4.5 最好被试

指所有 LOSO fold 中，测试准确率最高的那个被试。

例如：

```text
S4 0.8037
```

说明以 S4 作为测试被试时，模型表现最好。

作用：

- 看哪些被试比较容易被模型泛化；
- 分析该被试是否和其他训练被试分布更接近；
- 判断该被试数据质量是否更好。

### 4.6 最差被试

指所有 LOSO fold 中，测试准确率最低的那个被试。

例如：

```text
S2 0.5494
```

作用更重要，因为它可以帮助定位问题：

- 该被试可能个体差异较大；
- 该被试数据质量可能较差；
- 该被试标签分布可能特殊；
- 模型对该被试泛化能力不足。

### 4.7 平均 best epoch

每个 fold 都会记录在哪个 epoch 达到最高测试准确率。

例如：

```text
S1 best epoch = 60
S2 best epoch = 90
S3 best epoch = 40
```

平均后就是 `平均 best epoch`。

它回答的问题是：

> 模型一般训练到第几轮效果最好？

作用：

- 判断 200 epoch 是否必要；
- 辅助设置 early stopping；
- 判断是否后期过拟合。

如果平均 best epoch 很早，比如 30，但训练到 200，说明后面很多训练可能收益不大，甚至可能过拟合。

### 4.8 best-test时 train acc

这是指：

> 在测试准确率最高的那个 epoch，当时的训练准确率是多少。

例如某个 fold：

```text
epoch 78:
train acc = 0.47
test acc = 0.69  ← 最高 test acc
```

这里的 `train acc = 0.47` 就会参与计算 `best-test时 train acc`。

它回答的问题是：

> 当模型泛化最好时，它对训练集拟合到了什么程度？

这个指标有时会低于最终训练准确率，因为最佳泛化点不一定出现在训练集准确率最高的时候。

### 4.9 final train acc

`final train acc` 是最后一个 epoch 的训练准确率。

例如训练 200 epoch：

```text
epoch 200 train acc = 0.99
```

它回答的问题是：

> 模型最终对训练集拟合得有多好？

如果：

```text
final train acc 很高
test acc 没有同步提高
```

通常说明模型后期已经在记忆训练集，泛化能力没有同步提升，可能存在过拟合。

## 5. 当前结果分析

### 5.1 最优配置

加入 time 单分支和 fft 单分支作为对比后，整体最优仍然是：

```text
time+fft双分支 / window_15_stride_3
```

它的主要指标为：

```text
mean best acc = 0.6855
macro F1      = 0.5559
mean avg acc  = 0.5131
```

和几个关键对照相比：

```text
time+fft window_15_stride_3 : mean best acc = 0.6855, macro F1 = 0.5559
time+fft window_9_stride_9  : mean best acc = 0.6812, macro F1 = 0.5332
time单分支 3:3:1           : mean best acc = 0.6790, macro F1 = 0.4999
time单分支 1:1:1           : mean best acc = 0.6692, macro F1 = 0.4838
fft单分支                  : mean best acc = 0.5875, macro F1 = 0.4229
```

`window_15_stride_3` 相比最强的 time 单分支只高约 0.0065，但 macro F1 提升更明显，说明 time+fft 双分支对类别均衡性有帮助。

### 5.2 单分支与双分支对比

从总体结果看：

- time 单分支明显强于 fft 单分支；
- fft 单分支单独使用效果最差，且 `std = 0.1292`，跨被试波动最大；
- time+fft 双分支的最好配置优于 time 单分支，但不是所有 time+fft 窗口都优于 time 单分支；
- time+fft 的窗口长度和 stride 对结果影响明显，`15/3` 和 `9/9` 表现最好。

### 5.3 稳定性

从 `std` 看，最稳定的几个配置是：

```text
time+fft window_3_stride_3  : std = 0.0504
time+fft window_15_stride_15: std = 0.0509
time+fft window_9_stride_9  : std = 0.0511
time单分支 1:1:1            : std = 0.0538
```

`fft单分支` 的标准差最大：

```text
fft单分支: std = 0.1292
```

说明仅使用频域特征时，对不同被试的泛化稳定性较差。

### 5.4 过拟合现象

多个 time+fft 配置的 `final train acc` 都非常高，例如：

```text
time+fft window_15_stride_3 : final train acc = 0.9908
time+fft window_9_stride_1  : final train acc = 0.9917
time+fft window_15_stride_15: final train acc = 0.9870
```

但测试准确率并没有达到同等水平，说明模型后期对训练被试拟合很强，但跨被试泛化能力仍有限，存在一定过拟合现象。

相比之下，time 单分支和 fft 单分支的 final train acc 较低：

```text
time单分支 3:3:1: final train acc = 0.8913
time单分支 1:1:1: final train acc = 0.9102
fft单分支       : final train acc = 0.7832
```

这说明双分支模型容量更强，更容易把训练集拟合到很高，但也更需要关注泛化性能。

### 5.5 被试差异

S4 在多个配置中都是表现最好的或接近最好的被试，说明 S4 可能更容易被模型泛化识别。

S2 在多个配置中表现较差，特别是：

```text
time+fft window_3_stride_1: S2 = 0.3578
time+fft window_9_stride_1: S2 = 0.4661
time+fft window_9_stride_3: S2 = 0.4812
fft单分支 window_3_stride_3: S2 = 0.3030
```

说明 S2 可能存在更明显的个体差异、数据质量问题，或与其他被试的数据分布差异较大。

## 6. 简单总结

如果要选择当前最佳模型配置，优先考虑：

```text
activity_loso_time_fft/window_15_stride_3
```

原因：

- `mean best acc` 最高；
- `macro F1` 最高；
- `mean avg acc` 最高；
- 相比 time 单分支和 fft 单分支，综合表现最好。

如果更关注稳定性，可以同时关注：

```text
activity_loso_time_fft/window_9_stride_9
activity_loso_time_fft/window_15_stride_15
activity_loso_time_fft/window_3_stride_3
activity_loso_1:1:1_ep200/window_3_stride_3
```

整体结论：

- `time单分支` 是强基线，尤其是 3:3:1 权重配置；
- `fft单分支` 单独使用效果较弱，且跨被试波动较大；
- `time+fft双分支` 的最佳配置略优于 time 单分支，并且 macro F1 更好；
- 但 time+fft 并非所有窗口配置都更好，需要通过窗口长度和 stride 选择合适配置。

最终选模型时，建议优先看：

```text
mean best acc
macro F1
std
mean avg acc
```

分析训练过程和过拟合时，重点看：

```text
平均 best epoch
best-test时 train acc
final train acc
```

分析问题被试时，重点看：

```text
最好被试
最差被试
各被试 best test acc 表
```
