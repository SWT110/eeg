# EEG 活动三分类实验现状

本文根据 `local_artifacts/outputs` 下的训练产物，结合现有项目文档整理当前实验进展。重点以已经完成的 LOSO 训练结果为主，项目流程文档仅作为背景参考。

## 1. 实验目标与评估方式

当前实验目标是做 **EEG 活动三分类**，即输入一段 EEG 信号窗口，预测其属于活动 `e_1 / e_2 / e_3` 中的哪一类；不是识别受试者身份。

评估方式采用 **LOSO（Leave-One-Subject-Out，留一受试者法）**：

- 共 11 名受试者；
- 每个 fold 留 1 名受试者作为测试集，其余 10 名作为训练集；
- 每组配置完整训练 11 个 fold；
- 主要关注跨被试泛化能力。

当前每个 fold 主要产物包括：

- `best_model.pt`
- `metrics.json`
- `epoch_history.csv/json`
- `test_predictions.npz`
- `train.log`

主实验每个窗口配置还生成：

- `summary.json`
- `summary.csv`
- `experiment_manifest.md/json`

## 2. 数据集与窗口配置现状

当前已经构建了 7 套全局窗口级活动数据集，均位于：

```text
local_artifacts/data_to_list/global_activity_dataset/
```

| 数据集 | X 形状 | 样本数 | 类别计数 `0/1/2` |
|---|---:|---:|---:|
| `window_3_stride_1` | `(17647, 21, 384)` | 17647 | 3278 / 3476 / 10893 |
| `window_3_stride_3` | `(5897, 21, 384)` | 5897 | 1100 / 1166 / 3631 |
| `window_9_stride_1` | `(17449, 21, 1152)` | 17449 | 3212 / 3410 / 10827 |
| `window_9_stride_3` | `(5831, 21, 1152)` | 5831 | 1078 / 1144 / 3609 |
| `window_9_stride_9` | `(1951, 21, 1152)` | 1951 | 363 / 385 / 1203 |
| `window_15_stride_3` | `(5765, 21, 1920)` | 5765 | 1056 / 1122 / 3587 |
| `window_15_stride_15` | `(1173, 21, 1920)` | 1173 | 220 / 231 / 722 |

可以看到类别 2 样本明显更多，因此 accuracy 不能单独作为唯一指标，macro F1 和各类别召回率也需要重点关注。

## 3. 已完成实验规模

当前 `outputs` 中已经完成：

- 原 8 组主对照实验：`8 × 7 = 56` 组窗口实验；
- 附加 A3-QKV 实验：`1` 组，仅跑了 `window_15_stride_3`；
- 合计：`57` 组实验配置；
- 每组 11 个 LOSO fold，共 `627` 个 fold 训练产物。

产物数量统计：

| 产物 | 数量 |
|---|---:|
| `best_model.pt` | 627 |
| `metrics.json` | 627 |
| `epoch_history.csv` | 627 |
| `test_predictions.npz` | 627 |
| `summary.json` | 56 |
| `experiment_manifest.md` | 56 |

说明：A3-QKV 的 11 个 fold 已完成，但目前没有生成统一的 `summary.json` 和 `experiment_manifest.md`，其汇总结果来自各 fold 的 `metrics.json`。

## 4. 实验组设置

当前主实验共有 8 组：

| 组别 | 配置 | 含义 |
|---|---|---|
| A1 | `time + standard` | 时域单分支 baseline |
| A2 | `fft + standard` | FFT 单分支 baseline |
| A3 | `time_fft + standard` | 时域 + FFT 双分支 baseline |
| B1 | `time + DWConv` | 时域单分支 + 深度卷积 |
| B2 | `fft + DWConv` | FFT 单分支 + 深度卷积 |
| B3 | `time_fft + DWConv` | 双分支 + 深度卷积 |
| C1 | `fft + DWConv + MLP` | FFT 分支加入全局 MLP |
| C2 | `time_fft + DWConv + MLP` | 双分支，FFT 分支加入 MLP |

另有 1 组附加实验：

| 组别 | 配置 | 说明 |
|---|---|---|
| A3-QKV | `time_fft + standard + input_qkv=channel` | 仅在 `window_15_stride_3` 上测试 |

主 8 组实验训练设置基本一致：

- epochs：200
- batch size：72
- learning rate：0.0002
- optimizer：Adam
- class weights：`[3, 3, 1]`
- device：`cuda:0`
- seed：42

注意：A3-QKV 的 `metrics.json` 显示 `class_weights=null`，与主 8 组实验不完全一致，因此和 A3 主实验对比时需要谨慎。

## 5. 当前总体最优结果

按 `mean best acc` 排序，当前前 10 名如下：

| 排名 | 配置 | 窗口 | mean best acc | std | macro F1 | mean avg acc |
|---:|---|---|---:|---:|---:|---:|
| 1 | A1 time standard | `15/3` | **0.6948** | 0.0409 | 0.5428 | 0.5324 |
| 2 | A3 time+fft standard | `15/3` | 0.6855 | 0.0612 | **0.5559** | 0.5131 |
| 3 | A1 time standard | `3/1` | 0.6846 | 0.0945 | 0.5019 | 0.4076 |
| 4 | A1 time standard | `9/1` | 0.6812 | 0.0436 | 0.5035 | 0.4592 |
| 5 | A3 time+fft standard | `9/9` | 0.6812 | 0.0511 | 0.5332 | 0.4809 |
| 6 | A1 time standard | `9/9` | 0.6747 | 0.0440 | 0.5149 | 0.5279 |
| 7 | B1 time DWConv | `9/1` | 0.6733 | 0.0485 | 0.5302 | 0.4354 |
| 8 | A1 time standard | `9/3` | 0.6704 | 0.0445 | 0.5140 | 0.4845 |
| 9 | A1 time standard | `15/15` | 0.6668 | 0.0586 | 0.4619 | **0.5427** |
| 10 | A3-QKV | `15/3` | 0.6656 | 0.0608 | 0.4691 | 0.5120 |

当前如果只看平均最高测试准确率，最优配置是：

```text
A1 time standard / window_15_stride_3
mean best acc = 0.6948
```

如果更关注三分类的类别均衡性，最优配置是：

```text
A3 time+fft standard / window_15_stride_3
macro F1 = 0.5559
```

## 6. 各组最佳结果

| 组别 | 最佳窗口 | mean best acc | macro F1 | std |
|---|---|---:|---:|---:|
| A1 time standard | `15/3` | **0.6948** | 0.5428 | 0.0409 |
| A2 fft standard | `15/15` | 0.6325 | 0.4405 | 0.0649 |
| A3 time+fft standard | `15/3` | 0.6855 | **0.5559** | 0.0612 |
| B1 time DWConv | `9/1` | 0.6733 | 0.5302 | 0.0485 |
| B2 fft DWConv | `3/3` | 0.6285 | 0.3614 | 0.0439 |
| B3 time+fft DWConv | `15/3` | 0.6443 | 0.4478 | 0.0516 |
| C1 fft DWConv+MLP | `15/15` | 0.6259 | 0.3840 | 0.0294 |
| C2 time+fft DWConv+MLP | `15/15` | 0.6447 | 0.4067 | **0.0270** |
| A3-QKV | `15/3` | 0.6656 | 0.4691 | 0.0608 |

主要结论：

1. **A1 time standard 是当前最强 accuracy baseline**，尤其 `window_15_stride_3` 达到 0.6948。
2. **A3 time+fft standard 的 macro F1 最高**，说明融合 FFT 后对类别均衡性有一定帮助。
3. **FFT 单分支整体弱于 time 单分支**，说明当前任务中时域信息更关键。
4. **DWConv 和 MLP 变体目前没有超过 standard baseline**，部分配置虽然更稳定，但整体准确率和 macro F1 偏低。
5. **A3-QKV 未超过原 A3**，当前结果不支持继续大规模扩展 QKV，除非先补齐 class weights、summary 和更多消融。

## 7. 窗口长度与步长影响

按 8 组主实验在同一窗口下的平均表现看：

| 窗口 | 8 组平均 mean best acc | 该窗口最佳配置 |
|---|---:|---|
| `15/3` | **0.6398** | A1 time standard |
| `15/15` | 0.6354 | A1 time standard |
| `9/9` | 0.6282 | A3 time+fft standard |
| `9/1` | 0.6271 | A1 time standard |
| `9/3` | 0.6236 | A1 time standard |
| `3/3` | 0.6124 | A1 time standard |
| `3/1` | 0.6076 | A1 time standard |

整体上，`window_15_stride_3` 是当前综合最好的窗口设置；`window_9_stride_9` 对 A3 双分支也表现较好。短窗口 `3s` 虽然在 A1 `3/1` 上能达到较高 accuracy，但波动较大，稳定性不如 `15/3`。

## 8. 类别识别现状

以当前 accuracy 最优的 A1 `window_15_stride_3` 为例，整体混淆矩阵为：

```text
[[ 320, 225, 511],
 [ 137, 393, 592],
 [  94, 200, 3293]]
```

对应各类别指标：

| 类别 | precision | recall | F1 |
|---:|---:|---:|---:|
| 0 | 0.5808 | 0.3030 | 0.3983 |
| 1 | 0.4804 | 0.3503 | 0.4052 |
| 2 | 0.7491 | 0.9180 | 0.8250 |

这说明模型对多数类 2 识别较好，但对类别 0 和类别 1 的召回率仍偏低，很多少数类样本被预测为类别 2。因此当前实验虽然 accuracy 接近 0.70，但 macro F1 仍只有 0.54 左右，类别不均衡仍是主要问题。

## 9. 被试差异现状

从 57 组实验的各 fold 平均表现看，被试之间差异明显：

| 被试 | 平均 best acc |
|---:|---:|
| S4 | **0.7072** |
| S1 | 0.6922 |
| S7 | 0.6467 |
| S5 | 0.6365 |
| S6 | 0.6363 |
| S10 | 0.6340 |
| S8 | 0.6210 |
| S9 | 0.5922 |
| S11 | 0.5902 |
| S3 | 0.5884 |
| S2 | **0.5368** |

S4 和 S1 通常较容易泛化；S2 是当前最困难的测试被试，多个配置中都表现较低，后续需要重点检查 S2 的数据质量、标签分布和信号分布差异。

## 10. 过拟合与评估注意点

当前多个高排名实验的 `final train acc` 已接近 0.95–0.99，但测试集 `mean best acc` 仍在 0.65–0.70 附近，说明模型对训练被试拟合较强，跨被试泛化仍有限。

另外，当前 `experiment_manifest.md` 中显示：

```text
validation_strategy: none
best_model_selected_by: test_acc
```

这意味着目前结果更适合作为内部实验对比和配置筛选；如果要作为更严格的论文式结果，后续应引入验证集或嵌套验证策略，不能长期依赖测试集选择最佳 epoch。

## 11. 当前阶段结论

当前实验已经完成了较系统的 LOSO 对照：

- 数据集链路已打通；
- 7 套窗口/步长数据集已生成；
- 8 组主对照实验全部完成；
- A3-QKV 附加实验已完成 11 folds；
- 汇总结果显示时域特征仍是主导，FFT 融合主要改善 macro F1，但并未显著提升最高 accuracy；
- 当前最强配置集中在 `window_15_stride_3` 和 standard 结构。

阶段性推荐：

| 使用目标 | 推荐配置 |
|---|---|
| 追求最高平均准确率 | A1 `time standard / window_15_stride_3` |
| 追求类别均衡性 | A3 `time+fft standard / window_15_stride_3` |
| 追求较稳定但可接受的性能 | A1 `time standard / window_9_stride_1` 或 A3 `time+fft standard / window_9_stride_9` |

## 12. 下一步建议

后续建议优先做以下工作：

1. **补齐严格验证流程**：增加 validation split 或 early stopping，避免继续用 test acc 选 best epoch。
2. **针对类别不均衡优化**：尝试更强 class weights、balanced sampler、focal loss，重点提升类别 0/1 recall。
3. **重点分析 S2、S3、S11**：检查这些困难被试的数据质量、类别分布和信号差异。
4. **对最优配置做多 seed 重复实验**：当前主要是 seed=42，建议对 A1-15/3 和 A3-15/3 做多随机种子验证稳定性。
5. **补齐 A3-QKV 汇总与公平对比**：重新加上 class weights `[3,3,1]` 后再判断 QKV 是否有效。
6. **确定最终部署模型前先选定配置**：若用于实际预测，建议在确认最优配置后，再用全部 subject 训练 `activity_final` 模型。
