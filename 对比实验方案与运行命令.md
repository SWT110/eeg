# 对比实验方案与运行命令

## 1. 当前结论

导师要求包含“一两个经典模型”和 2025 年前后的新模型，并在当前 EEG
数据上实际训练。当前锁定以下四个基线：

| 模型 | 年份 | 角色 | DOI | 采用原因 |
|---|---:|---|---|---|
| EEGNet | 2018 | 经典轻量 CNN | `10.1088/1741-2552/aace8c` | EEG 分类中常用、参数少、容易复现 |
| ShallowConvNet | 2017 | 经典浅层 CNN | `10.1002/hbm.23730` | 具有滤波器组/对数功率归纳偏置，与 EEGNet 形成互补 |
| ATCNet | 2023 | Attention + TCN | `10.1109/TII.2022.3197419` | 与本文的时序建模路线接近，是较强中期基线 |
| TCFormer | 2025 | 多核 CNN + GQA/RoPE + TCN | `10.1038/s41598-025-16219-7` | 满足近期方法要求，有官方 PyTorch 代码和 LOSO 配置 |

TCFormer 论文及四模型统一实现来自官方仓库
<https://github.com/Altaheri/TCFormer>，适配基准为 commit
`701a361640708ef1cc07cf22660d47edc6d9bf3c`，MIT 许可与改动说明见
`EEG-Conformer/THIRD_PARTY_NOTICES.md`。论文 `references.bib` 已加入
TCFormer 的真实题录；其余三篇原本已经存在。

不选 EmT 或 Dual-TSST 作为第一批实际复现，不代表其文献价值较低，而是当前优先保证
官方实现可得、许可清楚、能在同一输入张量和 LOSO 协议下稳定训练。完成这四项后，
再根据导师意见决定是否扩展第五个模型。

## 2. 公平比较协议

本轮先复现与现有主结果完全可比的“开发性协议”：

- 数据：`window_15_stride_3`，形状 `5765 x 21 x 1920`，采样率 128 Hz。
- 划分：11-fold LOSO，每次完整留出 1 名受试者。
- 归一化：只用当前训练 fold 的全部数值计算一个标量 mean/std，并应用到测试 fold。
- 输入：四个外部基线都使用相同的时域窗口，不额外使用 FFT。
- 损失：加权交叉熵，类别权重 `[3, 3, 1]`。
- 优化器：Adam，学习率 `0.0002`，betas `(0.5, 0.999)`，weight decay `0`。
- 预算：batch size 72，200 epochs，第一轮 seed 42。
- 选择规则：每个 epoch 在留出受试者上评估，保留最高测试 accuracy 的 checkpoint。

最后一条会造成测试折参与 checkpoint 选择，因此结果只能与论文当前的开发性结果公平
比较，不能称为无偏确认性结果。投稿前的更严格版本应把 epoch 与超参数选择放入外层
训练受试者中的内层 subject-wise validation，并在未查看外层结果前锁定规则。

四个模型保留各自发表/官方代码中的架构默认值，例如 EEGNet 的 `F1=8, D=2,
F2=16`，ATCNet 的 5 个时间窗，以及 TCFormer 的核 `[20,32,64]`、5 层
Transformer、4 个 query heads/2 个 KV heads。原论文在运动想象数据、裁剪长度、滤波、
增强、学习率、scheduler 等方面与本项目不同；这些原论文结果不能与本项目数值直接排名。

统一学习率便于控制训练预算，但不保证每个基线都达到各自最优状态。若第一轮后要补
“作者推荐优化器/学习率”版本，必须单独标记，并在外层训练受试者的内层验证中以相同
搜索预算选择；不能看过留出受试者结果后只为某个模型调参，再把较高数字当作公平结果。

## 3. 已完成的本机验证

2026-09-04 已完成：

1. 四模型 `batch x 1 x 21 x 512` 的前向、交叉熵反向传播测试。
2. 原训练模块相关 149 个回归测试全部通过。
3. 真实 `window_15_stride_3` 数据 dry-run：识别 11 名受试者、44 个计划 fold。
4. 真实受试者 1、seed 42、每模型 1 epoch 的 CPU smoke run；四项均生成
   checkpoint、epoch history、逐窗预测、混淆矩阵和汇总文件。

Smoke 目录为：

```text
local_artifacts/outputs/activity_comparison_smoke/
```

其中任何 accuracy/F1 都不能写入论文，因为只有 1 名受试者、1 epoch。它只证明数据、
模型、反向传播、保存和汇总链路已打通。本机单 fold 单 epoch 的完成时间约为：EEGNet
30 秒、ShallowConvNet 2 分钟、ATCNet 55 秒、TCFormer 5 分钟；完整实验必须使用 GPU。

## 4. GPU 服务器正式运行

服务器既有路径记录为：

```text
/public/home/gd_110413/SWT/EEG
/public/home/gd_110413/SWT/EEG/EEG-Conformer/.conda-envs/eegconformer310/bin/python
```

本轮新增代码当前位于本地 `eeg` 工作区；先将相关变更审阅、提交并推送到该仓库的
`origin`，再到服务器同步并验证四模型计划。不要只执行服务器端 `git pull` 而漏掉
本地尚未推送的改动。

```bash
cd /public/home/gd_110413/SWT/EEG
git pull

cd EEG-Conformer
./.conda-envs/eegconformer310/bin/python train_activity_comparison.py \
  --dataset-root ../local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --models eegnet,shallowconvnet,atcnet,tcformer \
  --seeds 42 \
  --epochs 200 \
  --batch-size 72 \
  --lr 0.0002 \
  --class-weights 3,3,1 \
  --device cuda:0 \
  --dry-run
```

确认输出为 11 名受试者、44 个 fold 后，启动第一轮完整比较：

```bash
cd /public/home/gd_110413/SWT/EEG/EEG-Conformer
mkdir -p logs
nohup ./.conda-envs/eegconformer310/bin/python -u train_activity_comparison.py \
  --dataset-root ../local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --output-dir ../local_artifacts/outputs/activity_comparison \
  --models eegnet,shallowconvnet,atcnet,tcformer \
  --seeds 42 \
  --epochs 200 \
  --batch-size 72 \
  --lr 0.0002 \
  --class-weights 3,3,1 \
  --device cuda:0 \
  --resume \
  --skip-existing \
  > logs/activity_comparison_seed42.log 2>&1 &
```

查看进度：

```bash
tail -f /public/home/gd_110413/SWT/EEG/EEG-Conformer/logs/activity_comparison_seed42.log
```

中断后直接重复同一条 `nohup` 命令。`--resume` 会恢复模型、Adam 状态、历史与 RNG；
`--skip-existing` 只跳过配置完全一致且已有 `metrics.json` 的 fold。

第一轮 44 个 fold 完整后，再运行 5-seed 版本。由于 seed 42 会被正确跳过，只补跑
43--46：

```bash
cd /public/home/gd_110413/SWT/EEG/EEG-Conformer
nohup ./.conda-envs/eegconformer310/bin/python -u train_activity_comparison.py \
  --dataset-root ../local_artifacts/data_to_list/global_activity_dataset/window_15_stride_3 \
  --output-dir ../local_artifacts/outputs/activity_comparison \
  --models eegnet,shallowconvnet,atcnet,tcformer \
  --seeds 42,43,44,45,46 \
  --epochs 200 \
  --batch-size 72 \
  --lr 0.0002 \
  --class-weights 3,3,1 \
  --device cuda:0 \
  --resume \
  --skip-existing \
  > logs/activity_comparison_seeds42_46.log 2>&1 &
```

不要让两个进程同时写同一个 `--output-dir`，否则根目录的总汇总文件可能相互覆盖。
如果要使用两张 GPU 并行，应为两个进程使用不同输出根目录，完成后再做一次离线合并。

## 5. 输出和验收

正式根目录结构：

```text
local_artifacts/outputs/activity_comparison/
├── protocol.json
├── comparison_summary.json
├── comparison_summary.csv
├── eegnet/seed_42/fold_subject_1/...fold_subject_11/
├── shallowconvnet/seed_42/fold_subject_1/...fold_subject_11/
├── atcnet/seed_42/fold_subject_1/...fold_subject_11/
└── tcformer/seed_42/fold_subject_1/...fold_subject_11/
```

每个正式模型/seed 必须同时满足以下条件，才可进入第四章：

- `n_folds == 11`；
- 11 个 fold 的 `metrics.json` 均为 `epochs == 200`；
- architecture、seed、类别权重、学习率与 `protocol.json` 一致；
- 11 个 `test_predictions.npz` 均存在；
- 汇总包含 mean accuracy、fold SD、pooled accuracy、macro-F1、balanced accuracy；
- 日志无失败、CUDA OOM 或混用旧配置；
- 论文只从最终 JSON/CSV 自动导入或核对数值，不从终端手抄。

在上述验收完成前，`lunwen_latex/main.tex` 只引用这些方法并说明完整同数据结果尚不可用，
不增加任何外部基线数值或 SOTA 表述。
